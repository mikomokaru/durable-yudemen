# dev サーバ（pnpm dev）でのみ読み込まれる環境変数。VITE_ 接頭辞のみ client に公開される。
# 縮退テスト用フォルトインジェクション（ping blackhole・要件14）を dev で有効化する。
# 本番ビルドでは import.meta.env.DEV が false のため、この値に関わらず blackhole は除外される（要件14.4）。
VITE_PING_BLACKHOLE_DEBUG=1
