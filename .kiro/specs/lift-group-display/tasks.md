# Implementation Plan

前提：`lift-group-planning`（PR #28・`keepsAnchor` まで）がマージ済み、または本ブランチがその先端に載っていること。本 spec は client と domain を変え、engine は `Ordered` の撤去と `toWireTimer` の 1 項目だけ触る（Requirement 5）。

進め方は `lift-group-planning` と同じ——各 task を `[-]` で始め、終えたら `[x]` と実測を残す。チェックポイントは `pnpm typecheck` / `pnpm lint` / `pnpm test` / `pnpm fmt:check`。コミットは task ごと。

- [x] 0. naming ゲートの確認（design 末尾の表）
  - 実測・2026-09-05: 表の名前はユーザー承認済み（「名前承認」）。実装で表に無い公開関数が 2 つ増えた——`suggestedItemOf`（推奨 → 品目 → 茹で秒 → serveAt の一連。`boilSecondsOf` は非公開に戻した）と `compareArrival`（到着順の全順序。レールと群で一つの定義）。いずれも既存のインライン式に名を与えたもので新しい概念ではないが、規律上は事後承認を要る。
  - `OrderItemOrigin` / `TimerFact.orderItem` / `PREP_LEAD_MS` / `LiftGroup` / `GroupItem` / `liftGroups` / `visibleGroups` / `headOf` / `slotSuggestions` / `pairSlots` / `SlotSuggestion`（`role` / `phase`）/ `SuggestionView` / `RadialQueueItem` / `RadialMenu.queue` / `onSelectItem` / `ClientView.unitOrigins` / `slotOffsets` / `affinityToleranceDistance` / `slotDistance`（移設）。撤去：`suggestionTiming` / `SuggestionTiming` / `planAnchor` / `nextForSlot` / `itemOf` / engine の `Ordered`。
  - ユーザー承認を得てから task 1 へ。

- [ ] 1. ワイヤと engine — `TimerFact.orderItem`
  - [ ] 1.1 `src/domain/timer.ts` に `OrderItemOrigin` を定義し、`TimerFact.orderItem: OrderItemOrigin | null` を足す（doc は engine の `Ordered` から移す）
  - [ ] 1.2 `src/engine/timer.ts` の `Ordered` を削除。`Timer` は `TimerFact` から継承。`createTimer` の入力型を `TimerFact["orderItem"]` へ。`migrate.ts` / `start.ts` / `src/domain/order.ts` の注記 / `tests/core/{timer.example,sync.p4.property}.test.ts` の型参照を追随
  - [ ] 1.3 `src/engine/project.ts` の `toWireTimer` が `orderItem` を写す。`tests/core/to-wire-timer-adjustment.example.test.ts` を「`orderItem` を運ぶ」形へ（`WIRE_TIMER_KEYS` に加え、`toHaveProperty("orderItem", timer.orderItem)`）
  - [ ] 1.4 `src/domain/wire.ts` に `toOrderItemOrigin` を書いて export し、`toTimerFact` が読む（欠如 / null → null、非 null は `externalOrderId` 非空・`itemIndex` 非負整数・`tableId` null か非空文字列、逸脱は失敗）。`tests/domain/wireGenerators.ts` の `genTimerFact` に `orderItem` を足し、`wire.example` に欠如 → null と不正 → 失敗の例を足す
  - [ ] 1.5 `src/client/persistence.ts` の `toClientTimer` が `toOrderItemOrigin` で `orderItem` を復元する（永続は不正値も null に畳む・旧ブロブの優雅な移行）。`tests/client/persistenceCodec.property.test.ts` と `generators.ts` の Timer 生成器に `orderItem` を足す
  - [ ] 1.6 `src/client/connection.ts` の `decideLocalStart` が作る Provisional_Timer（`ClientTimer`）に `orderItem: null` を足す（永続からの復元とは別の生成経路。アドホック開始ゆえ常に null）。`tests/client/localAuthority.property.test.ts` / `decideView.property.test.ts` の期待値を追随
  - [ ] 1.7 チェックポイント（engine の遷移・計画・採点・永続のテストが変更なしに通ること＝Property 13）
  - _Requirements: 5.1, 5.2, 5.3_

- [x] 2. domain — `PREP_LEAD_MS` と `slotDistance` の移設
  - [x] 2.1 `src/domain/messages.ts` に `PREP_LEAD_MS = 60_000`（doc：麺を準備する猶予・設定にしない）
  - [x] 2.2 `slotDistance` / `position` / 2 定数を `src/engine/objective.ts` から `src/domain/store.ts` へ移す（doc ごと）。domain は `slotDistance` と `position` を export。`objective.ts` と `schedule.ts` は domain から import（`schedule.ts:713` の注記を domain へ）。再 export しない。`tests/core/objective.{property,example}.test.ts` の import 先を domain へ
  - [x] 2.3 静的検査：`objective.ts` に `function slotDistance` が無いこと（Property 7 / Requirement 6.7）
  - 実測・2026-09-05: `PREP_LEAD_MS` は `CookRecommendation` の直後に置いた（猶予が推奨の `startAt` に対してだけ意味を持つ時間ゆえ）。`slotDistance` / `position` / 2 定数を doc ごと `store.ts` の `defaultUnitOrigin` の直後（レイアウトの節の末尾）へ移し、`position` は engine の代表 slot 選定が座標を要るため export にした。算術は 1 文字も変えていない（AC 5.7）。`objective.ts` から `SLOTS_PER_UNIT` / `GridPoint` の import が消え、`schedule.ts` の `objective` からの import は型だけになった。注記の行は design の `:713` ではなく `:739`（task 1 で行がずれた）。静的検査は `tests/lift-group-display.static.test.ts`（spec 名で 1 ファイル・TypeScript AST）に (a) domain が `slotDistance` を export する・(b) `objective.ts` に `function slotDistance` / `position` の宣言が無い・(c) `objective.ts` と `schedule.ts` が `../domain/store` から import し `objective.ts` が再 export しない、の 3 件。**計画からの逸脱 1 点**：静的検査は `vitest.config.ts` の static project の `include` に足すだけでなく、workers project の `exclude` にも足す必要があった（workers は `tests/**/*.test.ts` を拾うため、`node:fs` を読む検査が workerd 上でも走って Worker が落ち、「227 passed (228)」＋ unhandled error になった）。既存の静的検査と同じく include / exclude を対で置いて解消。typecheck 0（`worker-configuration.d.ts` を除く）・lint エラー 0・fmt:check 通過・227 ファイル 1411 件通過（3 回連続。うち 1 回だけ別の 1 件が落ちる走行があり、直後の 3 回で再現しなかった——本 task の変更と無関係な時間依存の揺れとみる）。**レビュー追記**：静的検査の (b)(c) は `FunctionDeclaration` と `export { … } from` しか見ておらず、`export { slotDistance }`（moduleSpecifier 無し）・`export const slotDistanceAlias = …`・`const slotDistance = …` の再定義がいずれも緑のまま通った——最も自然な逃げ道が検査の外にあり、Property 7「定義がただ一箇所・入口が一つ」を固定していなかった。宣言は `declaredNamesOf`（関数宣言＋変数宣言の束縛名。分割代入の葉も含む）、export は `exportedNames`（export 修飾子つき宣言・`export { a as b }` の両名・`export default a`・`export *` / `export * as ns` は印 `*`）に合算して名で照合する形に改めた。`objective.ts` へ 8 種の逃げ道（素の再 export・別名の再 export・`export const` 別名・`const` 再定義・分割代入の再定義・`export *`・`export * as`・`export default`）を順に注入し、いずれも該当する検査が落ちることを実行して確かめた。件数は変わらず 227 ファイル 1411 件通過。
  - _Requirements: 5.7, 6.7_

- [x] 3. `ClientView` の 3 項目
  - [x] 3.1 `src/client/connection.ts` の `ClientView` / `EMPTY_VIEW` / config case に `unitOrigins` / `slotOffsets` / `affinityToleranceDistance` を足す。config case の注記を「読み手のできた 3 項目だけ持つ」に改める
  - [x] 3.2 `tests/client/connection.example.test.ts` に config 受信で 3 項目が写る例を足す
  - 実測・2026-09-05: `ClientView` に 3 項目を足し（doc は「釜の組が domain の `slotDistance` で距離を測るために読む・計画と同じ座標と尺度」）、`EMPTY_VIEW` は `defaultUnitOrigins(DEFAULT_UNIT_COUNT)` / `DEFAULT_SLOT_OFFSETS` / `DEFAULT_AFFINITY_TOLERANCE_DISTANCE`、config case は 3 項目を写す。注記は「写すのは読み手のできた 3 項目だけ・重み・許容幅（秒）は引き続き持たない」に改めた。`connection.example` の例は既定と見分けのつく値（原点 2 個・オフセット x=2・距離 24）を config で受け、3 項目が写ることと `Object.keys` が `EMPTY_VIEW` と一致すること（重み・許容幅・対応表を持たない）を見る。**計画からの逸脱 2 点**：(1) `ClientView` に必須項目を足すため、`EMPTY_VIEW` を spread しない完全形のリテラル 5 ファイル（`reconcile.property`・`degraded-slot-superimposition.{gate,resolution,display}.property`・`assignment-ui.example`）に 3 項目を足した（`unitOrigins` は各自の `unitCount` と整合させる）。`tests/client/generators.ts` の `genClientView` は `EMPTY_VIEW` 基点ゆえ壊れないが、`unitOrigins` だけは生成した `unitCount` に整合させた（config の生成器と同じ規律）。(2) `order-queue.example` の「config の追加項目を受け取ってもユニット総数と麺種プリセットだけ」は主張（キー集合が `EMPTY_VIEW` と一致）はそのまま通るが題と注記が嘘になるため、「釜の組に要る 3 項目」を足した文言に改めた。typecheck 0（`worker-configuration.d.ts` を除く）・lint エラー 0（警告は既存のみ・`generators.ts` の map-spread は HEAD でも同じ行に在る）・fmt:check 通過・227 ファイル 1412 件通過（+1 件）。**レビュー追記**：`tests/client/generators.ts` の `genServerMessage` の config 分岐の注記「計画の重み・許容幅・レイアウトは client の畳み込みが読まない（unitCount / noodlePresets だけが確定される）」が本 task で嘘になっていた（同コミットで `genClientView` の doc は改めたが、こちらは据え置いた）。「重み・許容幅（秒）は読まない・レイアウトと許容距離は釜の組が読むため写されるが、生成の分散は要らないので既定に固定する」に改めた。件数は変わらず 227 ファイル 1412 件通過。
  - _Requirements: 4.7_

- [x] 4. `liftGroups.ts` — 群・開始・連鎖・先頭・釜ごとの提案・釜の組（**足すだけ**・旧経路は残す）
  - [x] 4.1 `queueDisplay.ts`：`QueueSuggestion` に `serveAt` を足し、`boilSecondsOf` を export（`suggestionTiming` はまだ消さない——利用側の撤去は task 5 で一括）
  - [x] 4.2 `src/client/components/liftGroups.ts`（新規）：`GroupItem` / `LiftGroup` / `liftGroups(view, corrected)` / `visibleGroups` / `headOf` / `SlotSuggestion` / `slotSuggestions(visible, view, corrected)` / `pairSlots(slot, slotSpan, view)`。design Component 3 の擬似コードどおり。`occupied` は店舗全体の Timer（running / boiled とも）
  - [x] 4.3 `tests/client/generators.ts` に群を作る場面（卓・茹で秒・`startAt`・走行中の仲間：一致 / 不一致 / boiled / `orderItem: null`）を足す
  - [x] 4.4 `tests/client/liftGroups.property.test.ts`（新規）：Requirement 6.1（片方向の等号）・6.2 / 1.10（並べ替えと端末ローカル項目への不変）・6.3（同卓の仲間の `endTime` だけを分割点とする単調性）・6.5（群の境界）・6.8（全釜 idle）・6.9（先頭の一意・全品の `startAt` を過ぎても）・6.10（開始の事実・同卓の後の batch は偽）・6.11（degraded で空）
  - [x] 4.5 `tests/client/liftGroups.example.test.ts`（新規）：茹で 510 / 360 / 330 秒の同卓 3 品で 180 秒後も head は 510 秒の品目だけ。`pairSlots`：起点の釜が埋まっていれば null（`slotSpan` 1 でも）・許容距離の内側に足りなければ null・近い順と index で断つ
  - [x] 4.6 チェックポイント（新規モジュールとその検査だけが増え、既存はすべて通る）
  - 実測・2026-09-05: `QueueSuggestion` に `serveAt` を足し（doc は「等号は `suggestedItemOf` だけが計算する・注文参照は足さない」）、推奨から提案と品目を一度に組む `suggestedItemOf(view, recommendation)` を export した。`liftGroups.ts` は design Component 3 の擬似コードどおり（`GroupItem` / `LiftGroup` / `liftGroups` / `visibleGroups` / `headOf` / `SlotSuggestion` / `slotSuggestions` / `pairSlots`）。群の鍵は卓なしなら品目・卓ありなら (tableId, serveAt)、`started` は同卓・`endTime === serveAt`・`endTime > corrected` の 3 条件、`occupied` は店舗全体の Timer（running / boiled とも）で `slotSuggestions` と `pairSlots` が同じ `occupiedSlots` を読む。`pairSlots` は起点の釜が埋まっていれば `slotSpan` 1 でも null、近い順・同距離は index 順。旧経路（`suggestionTiming` / `nextForSlot` / `planAnchor`）と `slotDisplay` / `SlotBoard` / `SlotCard` は触っていない（task 5）。生成器は `genLiftView` / `genLiftCorrected` / `genLiftScene`（batch ごとに卓と serveAt を決めて茹で秒から startAt を逆算・仲間は match / mismatch / stray / foreign の 4 種・orphan / retired の推奨・境界の corrected）で、`generators.smoke` に場面を踏む番人 2 件を足した。Property は 6.1・6.2 / 1.10（並べ替えと端末ローカル 7 項目への不変）・6.3（分割点の手前まで進めて消えない・跨ぐと接頭辞に縮む）・6.5・6.8（+2.14）・6.9（全品の startAt を過ぎても同じ先頭）・6.10（同卓の後の batch は偽）・6.11 の 9 件、Example は 510 / 360 / 330 秒の再現・連鎖の解禁・卓の一致だけ / endTime の一致だけでは偽・599 → 600 秒の転移・群に入らない推奨・卓なしは 1 品 1 群・複数釜と boiled の占有・`pairSlots` 4 件の 14 件。**計画からの逸脱 3 点**：(1) design は「`boilSecondsOf` を export して共用」としたが、レールと群はどちらも「推奨 → 品目 → 茹で秒 → serveAt」の連鎖を要るため、`boilSecondsOf` ではなく連鎖そのものを `suggestedItemOf(view, recommendation): { order, suggestion } | null` として一つだけ置き、`orderQueueEntries` と `liftGroups` の両方がそれを呼ぶ。`boilSecondsOf`（署名は `(presets, order)`——茹で秒は品目の事実からの導出）と推奨 → 品目の突き合わせ `pendingItemOf`（`itemKey` を使う）は非公開のまま。(2) 到着順の全順序を `compareArrival` として `queueDisplay.ts` から export した（レールの並びと群の中の同値の並びが同じ順序を要る・AC 1.4）。`suggestedItemOf` / `compareArrival` は naming の表に無い公開関数だが、既存の内側の式を名にしただけで新しい概念境界を立てていない——要確認なら task 5 でまとめて。(3) `QueueSuggestion` に必須項目を足すため、リテラルを組む既存テスト 3 本（`slot-card.example` / `order-rail.example` / `order-queue.example`）に `serveAt` を足した（型の追随・主張は不変）。typecheck 0（`worker-configuration.d.ts` を除く）・lint エラー 0（警告は既存のみ・場面の組み立てを `liftViewOf` に出して `map` 内の spread を増やしていない）・fmt:check 通過・229 ファイル 1437 件通過（3 回連続・+2 ファイル +25 件）。**レビュー追記**：serveAt の等号 `startAt + boilSeconds × 1000` が `orderQueueEntries` と `liftGroups` の 2 箇所にインラインで在り、`boilSecondsOf` の doc は「同じ関数で serveAt を再計算する」と述べながら茹で秒しか返していなかった（二つの真実と構造に合わない注記）。連鎖を `suggestedItemOf` に一本化し、両者からインラインの等号を消し、`pendingItemOf` / `boilSecondsOf` を非公開に戻した。Property 1 の照合は `boilSecondsOf` ではなくプリセットから直に茹で秒を引く（導出側の関数を照合に使えば等式が空になる）。件数は変わらず 229 ファイル 1437 件通過。
  - _Requirements: 1.1〜1.10, 2.1〜2.14, 6.1〜6.3, 6.5, 6.8〜6.11_

- [x] 5. 切り替え——旧経路の削除・表示型の変更・呼び出し側・描画・旧テストの撤去を **一つの作業単位**で行う（途中の状態では検査が通らないため、チェックポイントとコミットはこの task の末尾に一つ）
  - [x] 5.1 `slotDisplay.ts`：idle の `next: readonly SlotSuggestion[]`。第 4 引数を `bySlot: ReadonlyMap<number, readonly SlotSuggestion[]> = NO_SUGGESTIONS`（省略可）にし、`nextForSlot` を削除
  - [x] 5.2 `useAudioCues.ts:199` と `[]` を渡すテスト（`complete.example`・`audioCue.property`・`assignment-ui.example`・`localAuthority.property`・`degraded-slot-superimposition.{display,exploration}`）の第 4 引数を落とす。静的検査 `tests/sync-set-batch-complete.static.test.ts` の正規表現が `SlotBoard` の呼び出しで満たされることを確認
  - [x] 5.3 `SlotBoard`：`planAnchor` と時期の語（`in` / `+`・`formatRemaining` の提案での使用）を撤去。`corrected` を一度読み、`liftGroups` → `visibleGroups` → `slotSuggestions` → `assignedSlotDisplays`。`suggestionOf` は `SuggestionView`（`role` で判別）を返す。`itemOf` を削除し `item.order` から鍵を取る
  - [x] 5.4 `SlotCard`：`display.next` と `SuggestionView[]` を対で受け、`role` で分岐。`head` だけ丸ボタン（薄は opacity・濃は現行の塗り・語は `now` のみ）、`member` はラベルだけ。aria-label は `now` / `soon` / `queued`
  - [x] 5.5 `queueDisplay.ts` から `suggestionTiming` / `SuggestionTiming` を削除
  - [x] 5.6 旧テストの撤去：`tests/client/suggestionTiming.property.test.ts`（削除する関数を import）・`lapsedSuggestion.example.test.tsx`（旧表示の `in` / `+` を要求）・`slotSuggestion.{example,property}.test.ts`（`nextForSlot` / `itemOf`・task 4 の性質へ置き換え済み）
  - [x] 5.7 `tests/client/slot-card.example.test.tsx` を書き換え：`head`（薄・濃）に丸ボタンと aria-label、`member` にボタン無し・濃くない、複数の提案の折り返しで Start が右下に留まる、時刻の語が無い（Requirement 6.4）。Requirement 6.6（担当外の空白）と「units A と B で共通する釜の `next` が一致」を `assignedSlotDisplays` の Property として足す
  - [x] 5.8 チェックポイント（ここで初めて全体が通る）
  - 実測・2026-09-05: `slotDisplay.ts` の idle は `next: readonly SlotSuggestion[]`（空配列は提案なし）、第 4 引数は `bySlot: ReadonlyMap<number, readonly SlotSuggestion[]> = NO_SUGGESTIONS`（省略可・既定は空 Map）で `nextForSlot` と `mode` / `QueueEntry` の import を撤去した。`useAudioCues.ts` と `[]` を渡していたテスト 6 本（`complete.example`・`audioCue.property`・`assignment-ui.example`・`localAuthority.property`・`degraded-slot-superimposition.{display,exploration}`）は第 4 引数を落とし、静的検査 `sync-set-batch-complete.static` の正規表現は `SlotBoard` の `assignedSlotDisplays(view, units, now, bySlot)` が満たす（変更なし）。`SlotBoard` は `corrected` を一度組み、`liftGroups` → `visibleGroups` → `slotSuggestions` → `assignedSlotDisplays` と導き、`planAnchor` / `itemOf` / `formatRemaining` / `suggestionTiming` の使用を消した。`suggestionOf(suggestion, slot, colorOf): SuggestionView` は `role` で判別し、ラベルは `name size / firmness / Table n / now（head かつ solid のみ）`、aria-label は `Suggested — {name} · Slot {n} · now|soon|queued`。開始は `item.order` から鍵を取り `item.suggestion.slotIds` 全体で `startOrderItem`。`SlotCard` は `display.next` を `map` し、`head` の分岐だけが丸ボタン（薄は `opacity-60`・濃は現行の塗り）を描いて `onStartSuggested(item)` を呼び、`member` の分岐は `actionSlot` の枠を空のまま残してラベルだけ描く（ボタンの JSX が無い・aria-label はラベルの span へ）。相の事実は `data-phase="solid" | "faint"` として DOM に残す（`data-unconfirmed` と同じ判断）。`Start` と `actionRow` の配置は不変（AC 3.5）。`queueDisplay.ts` から `suggestionTiming` / `SuggestionTiming` を削除し、`QueueSuggestion` の doc を「担当範囲で絞る前の形・絞るのは呼び出し側」に改めた。旧テスト 4 本（`suggestionTiming.property`・`lapsedSuggestion.example`・`slotSuggestion.{example,property}`）を削除。`slot-card.example` は `head`（薄・濃）の丸ボタンと aria-label・`member` のボタン無しと濃くない・head と member の併存で押せるのは head だけ・複数の提案で DOM 順 `[提案…, Start]` と親の下端固定 / 折り返し / 右寄せ・時刻の語の不在（Requirement 6.4）・押下で `GroupItem` が同一性で渡る、の 11 件に書き換えた。Requirement 6.6（表示できる群が触れないユニットだけを担当にすれば idle の `next` はすべて空。対照として店舗全体では提案が出る場面に限る）と「units A と B で共通する釜の表示が一致し、idle の `next` は `bySlot` そのもの」を `tests/client/slotDisplay.property.test.ts`（新規）に置いた。**task 4 レビューの持ち越し 3 点**：(a) `liftGroups.example` に同じ釜へ 3 件（started な G1 の残り 2 品と別卓の G2）が並ぶ例を足し、受信順を降順にしても配列が startAt 昇順で role が `[head, member, head]` であることを固定した。(b) 「走行中の釜 0 には何も出ない」の例は釜 0 を指す推奨が無く空虚だったので、表示できる群 G2 に釜 0 を指す品目 `extra` を足し、群には在るが釜 0 には出ないことを見る形に改めた。(c) `generators.smoke` の群の境界の番人は `findIndex` の −1 を真と数えていたので、`stop !== -1 && stop < length − 1` に改めた。**計画からの逸脱 2 点**：(1) design は「`SlotCard` は `display.next` と `SuggestionView[]` を対で受け」としたが、並行する 2 配列は長さの食い違いを表現できるため、`noodleColor` と同じ形の resolver `suggestionOf: (suggestion: SlotSuggestion) => SuggestionView` にした（prop 名は据え置き・`onStartSuggested` とともに必須 prop へ）。`SuggestionView` は消費側の `SlotCard.tsx` に置いて export し、`SlotBoard` が import する（逆向きだと型だけの循環になる）。(2) `tests/client/audioGenerators.ts` と `complete.example` の idle リテラル（`next: null`）を `[]` に追随し、`complete.example` の `SlotCard` 描画には呼ばれない resolver（呼ばれたら前提違反で throw）を渡した。typecheck 0（`worker-configuration.d.ts` を除く）・lint エラー 0（警告は既存のみ）・fmt:check 通過・226 ファイル 1415 件通過（3 回連続・−4 ファイル +1 ファイル・−22 件）。最初の 1 走行だけ `wire-decode-failure.integration`（本 task が触っていないログ行数の検査）が落ち、直後の 4 回で再現しなかった——task 2 の実測と同じ時間依存の揺れとみる。**レビュー追記**：3 点。(1) `slot-card.example` の「時刻の語を持たない（6.4）」はテスト自身が書いた resolver の文字列を検査しており、`SlotBoard.suggestionOf` に `in` / `+` / mm:ss を足しても aria-label の相を可視の語と食い違わせても落ちなかった（AC 3.1 / 3.4 も主張だけで反証経路が無かった）。`tests/client/slot-board-suggestions.example.test.tsx`（新規・render プロジェクト）で SlotBoard を Testing Library で実描画し、本物の `suggestionOf` の語（ラベル末尾は `Table n` か `now`・aria-label 末尾は `now` / `soon` / `queued`・可視の `now` ⇔ aria の `now`）、押下で `connection.startOrderItem` が推奨の `slotIds` 全体 `["0","1"]` と `{ externalOrderId, itemIndex: 2 }` で 1 回呼ばれ `playTouchCue` が鳴ること、units `[0]` と `[0,1]` で釜 0〜5 の見え方が一致すること、担当外の空白（6.6）を DOM で固定した。塗りだけは happy-dom が `oklch` のインラインスタイルを捨てるため DOM で読めず、`audioWiring.example` と同じ形で `SlotCard` の props を控えて resolver の `tint` を麺色の resolver と突き合わせる。design Testing Strategy の 6.4（Property 4）はこのファイルが担い、`slot-card.example` の Validates から 2.5 / 6.4 を外した。4 種の変異（ラベルに `in 1:30`・aria だけ `now`・押した釜だけ送る・member の group から aria-label を落とす）を順に注入し、いずれも落ちることを実行して確かめた。(2) member の aria-label を role を持たない `span` に置いていた——ARIA 1.2 は generic への aria-label を禁じ、支援技術が無視するので AC 3.4 / 3.7 は member について実機で満たされていなかった。提案 1 件のスタック div を `role="group"` にして head / member とも aria-label をそこへ置き（head のボタンは自分の aria-label を別に持つ）、`slot-card.example` の `getByLabelText` を `getByRole("group", { name })` に改めた。(3) resolver 形にしたのに `SuggestionView` が `role` / `phase` を持ち、分岐は `shown.role`・押下は `suggestion.item` と判別が二箇所に写っていた（resolver が member に `head` を返せば「押せないのにボタン」が描ける）。`SuggestionView` を `{ label, ariaLabel, tint }` の 1 形にし、ボタン・opacity・`data-phase` はすべて導出の `suggestion.role` / `phase` から取る。`SlotBoard.suggestionOf` の `solid` 変数は消し、相の語 `phrase` を一度だけ組んで aria-label と可視の `now` の両方をそこから導く（食い違いが表現不能）。`slot-card.example` に「resolver が member に `now` の語を返してもボタンは現れず薄いまま」の例を足した。design Component 5 に実装注記（判別共用体は resolver 形と両立しない・1 形の理由・group の所在）を追記。**計画からの逸脱 1 点**：`tests/sync-set-batch-complete.static.test.ts` の「SlotCard に一括を示す操作要素が無い」が語 `group` を字面で拒んでおり `role="group"` で落ちた。眼目（一括完了の操作要素を足さない）は保ち、ARIA の role 値だけを除く形 `/(?<!role=")\bgroup\b/i` に改めた（task 1 の `TimerFact` キー集合の静的検査と同じく、他 spec の正当な拡張として追随）。typecheck 0（`worker-configuration.d.ts` を除く）・lint エラー 0・fmt:check 通過・227 ファイル 1423 件通過（+1 ファイル +8 件・2 回連続）。
  - _Requirements: 2.2〜2.5, 2.12, 3.1〜3.7, 6.4, 6.6, 7.3_

- [x] 6. `RadialMenu.tsx` — 待ち行列の帯
  - [x] 6.1 `RadialQueueItem` / `queue` / `onSelectItem` を足し、帯を花びらの反対側（`base` から導く・余白が足りなければ弧の下）に描く。`slotIds === null` の行は不活性。プリセットの花びらと幾何は変えない
  - [x] 6.2 `SlotBoard`：`picker` が開いたとき `orderQueueEntries` の各行に `pairSlots(picker.slot, order.slotSpan, view)` を付けて渡す。degraded では `queue: []`。選択 → `connection.startOrderItem(slotIds, { externalOrderId, itemIndex })`
  - [x] 6.3 `tests/client/radial-queue.example.test.tsx`（新規）：live で帯が出る・到着順・`slotSpan` 2 で足りない行は不活性・degraded で帯が無い・**開いたまま snapshot で起点の釜が走行中になると全行が不活性で `startOrderItem` が呼ばれない**
  - [x] 6.4 チェックポイント
  - 実測・2026-09-05: `RadialMenu` に `RadialQueueItem { order, slotIds: NonEmptyArray<string> | null }` と props `queue` / `onSelectItem` を足し、帯を `<ul aria-label="Waiting orders">` として花びらの反対側に描く。配置は `base` から導く（`base === 0` → 帯の `right` を `calc((vw − (cx − radius))px + 2.875rem)`、`base === Math.PI` → `left` を `calc((cx + radius)px + 2.875rem)`。花びらの半径 2.875rem は rem のまま calc で足し、px に写さない）。幅は `min(12rem, 余白)`、余白が 8rem に満たなければ弧の下（上端 `cy + radius + 花びら半径`・中心揃え・横幅でクランプ）。`overflow-y: auto`・開閉の opacity は花びらと同じ `shown`。行は `displayName · 茹で加減 · Table n`（時刻なし）で、麺種色は花びらと同じ resolver。`slotIds === null` の行は `aria-disabled` と `opacity-40` で、**onClick を持たない**（押しても何も起きないのは分岐ではなく口の不在による）。0 件（待ち行列が空・degraded）は帯そのものが不在で、描くか否かは `queue.length > 0` の 1 箇所。花びらの JSX と幾何（`cx` / `cy` / `base` / `spread` / `petals`）は 1 文字も変えていない（AC 4.6）。`SlotBoard` は `picker !== null && mode(view) === "live"` のときだけ `orderQueueEntries` の各行に `pairSlots(picker.slot, order.slotSpan, view)` を付けて渡し（それ以外は `[]`）、選択で `connection.startOrderItem(slotIds, { externalOrderId, itemIndex })` → `setPicker(null)` → `playTouchCue()`。`view.timers` は SlotBoard が直接読まず（`pairSlots` の中）、`sync-set-batch-complete` の静的検査はそのまま通る。`tests/client/radial-queue.example.test.tsx`（新規・render プロジェクト）は SlotBoard を実描画し、接続の作り物が snapshot の到着（購読者への通知）を再現する形で 9 件：到着順（配列は逆順に置く）と語・レールと同じ名（半角カナ → 全角も同じ）・`slotSpan` 1 は押した釜だけで `(a, 0)`・`slotSpan` 2 は釜 0 から `["0","1"]`、釜 4 からは同距離の 2・5 のうち index の小さい `["4","2"]`・釜 1〜3 が走行中の台で 2 釜要る行だけ不活性で押しても何も起きず閉じもしない・degraded で花びらだけ・**開いたまま釜 0 を占める snapshot で全行（`slotSpan` 1 を含む）が不活性になり選んでも要求しない**・逆に隣が空く snapshot で組めるようになる（帯は状態でなく導出）。「組めない行でも `onSelectItem` を呼ぶ」の変異で 2 件が落ちることを実行して確かめた。**計画からの逸脱 3 点**：(1) design の `onSelectItem: (item: RadialQueueItem) => void` は親に null の検査を強い、帯の「呼ばない」と親の「null なら返る」が同じ事実の二つの守りになる（最初の実装で変異検査が親の守りに吸われて検出できなかった）。引数の型を `RadialQueueItem & { slotIds: NonEmptyArray<string> }` に絞り、親は null を受けない（名は表のまま・型だけ狭い）。(2) 品目の表示名（商品名の代替・NFKC・麺量）が `OrderRail.displayName`（非公開）と `SlotBoard.suggestionOf` のインラインの 2 箇所に在り、帯で 3 箇所目になるため `queueDisplay.ts` に `displayName(order)` として一つだけ置き、3 者がそれを呼ぶ。表に無い公開関数だが既存の非公開関数を公開位置へ移しただけで、`suggestedItemOf` / `compareArrival` と同じく事後承認を要る。副作用として提案の aria-label も `displayName` で品目を呼ぶ（麺量があれば添える。従来は可視のラベルにだけ麺量が付いていた——既存テストは麺量なしの品目で aria-label を固定しており主張は不変）。(3) design の「幅は中心のクランプ余白（`margin = radius + 60`）から `min(12rem, 余白)`」は、余白を `margin` と読むと `cx = margin` のとき帯が画面外に出る（反対側に残るのは `margin − radius − 花びら半径 ≈ 14px`）。余白は「画面端から弧の外縁までの実際の距離」と読み、それが 8rem に満たなければ弧の下へ落とす（既定の半径 132 では画面端 3 割ほどの帯域で下に落ちる）。typecheck 0（`worker-configuration.d.ts` を除く）・lint エラー 0（警告は既存のみ）・fmt:check 通過・228 ファイル 1432 件通過（+1 ファイル +9 件）。**レビュー追記**：2 点。(1) 逸脱 (3) で側の余白を実距離に改めたのと同じ罠が縦軸に残っていた——逃がす先を「弧の下」に固定すると、中心は `margin`（192）で画面内へクランプされるだけなので下段の釜から開いたとき帯の上端は `cy + radius + 花びら半径 = vh − 14` に落ち、`maxHeight` は 14px（1 行 ≈ 34px も出ない）。既定 1024×768 では画面端から約 306px の帯域で側に置けず、右列・下段の釜の Start はそこに在るため、右下の釜では AC 4.1 が実機で満たされていなかった。9 件の Example は happy-dom の矩形が (0, 0) で `cx = cy = 192` の 1 点しか描かず検出できなかった。`belowArc` を `verticalColumn(cx, cy, radius, petalRadius, width, vw, vh)` に改め、上の余白 `cy − radius − 花びら半径` と下の余白 `vh − (cy + radius + 花びら半径)` を比べて広い側に置く（下なら `top`・上なら `bottom` を弧の外縁に揃え、`maxHeight` はその余白。同値は下）。`ColumnLayout` は `{ at: "side", edge: left|right, inset, top, height, width } | { at: "vertical", edge: top|bottom, inset, left, height, width }` で、どちらも edge 側の縁だけ花びらの半径を rem の calc で足す。`radial-queue.example` に `HTMLElement.prototype.getBoundingClientRect` を差し替えて Start の中心を四隅・左右の中段に置く 5 件を足し、帯の style（`calc(Npx + Rrem)` を含む）を矩形へ読み戻して画面内・高さ 8rem 以上・弧の外縁の円と重ならない、右下では上に置かれ下端が弧の上端 398px に揃う、を固定した。「常に下」の変異で 2 件が落ちることを実行して確かめた。design Component 6 に実装注記を足した。(2) 逸脱 (2) の副作用（提案の aria-label が `displayName` で麺量込み）は design（Component 5・`ariaLabel = Suggested — ${name} …` で可視だけ `name size`）と食い違ったまま、既存テストが麺量なしの品目だけで反証経路が無かった。麺量込みを採る——麺量の違う同名の品目は別の品目で、支援技術にだけ麺量を落とす理由が無く、規則の定義を `displayName` の 1 箇所に保てる。design Component 5 の擬似コードを `displayName(order)` に改めて実装注記を足し、`slot-board-suggestions.example` の先頭の品目に `sizeName: "L"` を与えて aria-label `Suggested — Salt L · Slot n · now|soon` の末尾までを固定した（aria-label だけ麺量を落とす変異で 4 件が落ちることを実行して確かめた）。typecheck 0（`worker-configuration.d.ts` を除く）・lint エラー 0（警告は既存のみ）・fmt:check 通過・228 ファイル 1437 件通過（+5 件）。
  - _Requirements: 4.1〜4.9, 6.11_

- [x] 7. 横断の検証（engine を実走させた snapshot を client の導出に通す）
  - [x] 7.1 `tests/client/liftGroups.crosslayer.example.test.ts`（新規）：`startOrderItemTimer` → `fireDueTimers` → `toWireSnapshot` で得た snapshot を `liftGroups` / `slotSuggestions` に通す。茹で上がりの 2 場面（同じ snapshot で 599 → 600 秒・その後の発火 snapshot）で「G2 が隠れる」が両方で成り立つこと。boiled の釜が index 最小 / 最大の両方。容量分割（6 釜・4 品・各 2 釜）の (i)(ii)——(ii) は再統合後に 3 品とも head
  - [x] 7.2 同ファイルに `receivePlan` で合流一片を採用 → 無関係な Timer を `startTimer` → snapshot を `liftGroups` に通し、合流分の群が `started`、届かなくなった一片の群が `started` でない（`keepsAnchor` の帰結）
  - 実測・2026-09-05: `tests/client/liftGroups.crosslayer.example.test.ts`（新規・workers プロジェクト）は snapshot を一切手書きせず、`decide`（アドホック開始・到着・品目からの開始・発火・完了・計画受領）が Broadcast した snapshot を `decideView`（Connectivity up → config → snapshot・receivedAt = serverTime ゆえ offset 0）に通し、`liftGroups` / `visibleGroups` / `headOf` / `slotSuggestions` を導く。engine と client は同じ `SYNC`（arms 2・許容 10%）と同じ 3 麺種（600 / 360 / 300 秒）を params と config で受ける。期待値は群（卓・serveAt 秒・品目・started）・表示できる群・先頭・釜ごとの提案（`"名 solid|faint|member"`）・snapshot の推奨（名・釜・startAt 秒）・実効 endTime をすべて秒の具体値で固定した 17 件。**(1) 茹で上がりの 2 場面**：同卓 t-1 の A（600 秒）と B（360 秒）、別卓 t-2 の C（300 秒・2 釜）。3 釜をアドホック麺茹でで塞ぎ（塞がなければ C が最早の群になる）、A を始めると B は錨 600 秒に合流（startAt 240）、C は釜の空く 600 秒に置かれる。599 秒で G1 started・G2 も見える（B solid・C faint）、600 秒（同じ snapshot）で G1 が started でなくなり C が消えて B だけが濃く残る、発火 snapshot で B は serveAt 960 秒（走行中と不一致・started 偽）で届き G2 は隠れる、A の Complete で B が現れる——boiled の釜が index 最小（釜 0）と最大（釜 5）の両変種で同じ 4 段。残りの見え方だけが変種で違う：最小では再計画が B を boiled の釜 0 へ置き直すため発火 snapshot の提案は空（Complete まで出ない・design Error Handling どおり）、最大では B が自分の釜 2 に留まり濃く出る。塞ぐ釜の 1 つは 900 秒で上がり、G2 の serveAt と偶然一致する卓なしの Timer を engine の実物で踏む（卓の一致を要る判定の反証経路）。**(2) 容量分割**：1 注文 4 品（360 秒・各 2 釜）。到着直後は batch 1 の 3 品（serveAt 360）と釜 0・1 を待つ item4（720）の 2 群、item1 を始めると合流する 2 品が G1（started・head 両方・solid）、item4 は G2 の唯一の head で、Prep_Lead が来ても釜 0・1 が走行中の間は出ない。(i) 2 品を始めて発火 → 全 boiled・G1 は存在せず item4 の群が先頭、Complete 前は釜 0・1 が boiled で空、Complete で釜 0・1 に head・solid。(ii) 始めないまま発火 → 3 品が startAt 360 / serveAt 720 の一群に再統合され 3 品とも head、item2 は釜 0・1（boiled）で出ず item3 / item4（釜 2〜5）は Complete 前から solid、Complete 後は 6 釜すべてに出る。**(3) keepsAnchor の帰結**：同卓 t-a に走行中 S（600 秒・釜 5）、空きは釜 0・1 だけ。自前解は先着の P（2 品・300 秒）を合流させ Q（2 釜・300 秒）を 900 秒へ置くが、外部計画（Q を合流・P を 900 秒）は卓の遅れの和が小さく採用される（採用直後：Q の群 started・同卓の P の群は偽）。無関係なアドホック麺茹でを釜 1 で始めて S の実効 endTime を +5 秒（605）に動かすと、採用済み一片は状態に残ったまま合成が捨て、自前解が p#0 を 605 秒に合流させて started（605 = S の wire endTime）。逆に 290 秒に 280 秒の麺茹でで −30 秒（570）に動かすと、Q（startAt 300）は 570 秒に届かず押し出しでもないので一片が残り、群の serveAt 600 ≠ 570 で started でない（正当な後続の batch）。変異検査：`started` の `endTime > corrected` を `>=` にして 2 件、卓の一致を外して 6 件が落ちることを実行して確かめた（卓の一致の反証は 900 秒の Timer を足すまで 0 件だった）。**engine の実測が design の記述と違った 2 点**（期待値は engine に合わせ、意味は design どおり）：(a) (i) で同時に始めた 3 本を Boil_Sync（arms 2）が 2 本＋1 本の Sync_Set に割って 324 / 324 / 396 秒へ散らすため「360 秒に 3 本が boiled」は 1 回の発火では成らない——Alarm の実際の連鎖どおり 324 秒と 360 秒に 2 回発火させ（324 で 2 本を凍結・残る 1 本は単独で 360 に戻る）、3 本の実効 endTime は 324 / 324 / 360。client の結論（item4 の群だけが先頭・釜 0・1）は design のまま。(b) 推奨の `slotIds` の並びは engine の解放時刻順（`["5","4"]`）で釜番号順ではない——client は各釜に同じ提案を載せるだけなので見え方に差は無い。**場面の組み立てで必要だった判断 2 点**：(c) index 最小の変種では現場が A を推奨（釜 4）と違う釜 0 で始める（engine は推奨との一致を検査しない・観測事実 12）。開始前は A と B が最も近い釜の組に置かれるので、A の釜が空き釜より小さい index になる経路は「推奨と違う釜を押す」しかない。C を 2 釜にしたのも同じ理由——1 釜なら同点の釜を index で断つ再計画が C を A の釜（走行中）へ置き、599 秒で G2 が `visibleGroups` には在るが `slotSuggestions` には出ず、「G2 の提案が消える」を釜の提案で言えない。(d) (3) の無関係な Timer は空いている釜が計画上の 0・1 しか無いため釜 1 を占め、Q の提案は全釜 idle で隠れる（主張は `started` に限る）。typecheck 0（`worker-configuration.d.ts` を除く）・lint エラー 0（警告は既存のみ）・fmt:check 通過・229 ファイル 1454 件通過（+1 ファイル +17 件）。全量 6 走行のうち 2 走行で `tests/shell/wire-decode-failure.integration`（本 task が触っていないログ行数の検査）の 1 件だけが落ち、単独では 3 回連続で通った——task 2・5 の実測と同じ時間依存の揺れとみる。
  - _Requirements: 1.7, 6.3, 6.10_

- [x] 8. 文書の整合（本体と同じコミット）
  - [x] 8.1 `slot-suggested-start` の Requirement 1.2・2.3〜2.5・判断 5 に本 spec が改めた旨を追記
  - [x] 8.2 `online-cook-scheduling` design の「`unitOrigins` / `slotOffsets` は現時点で用途なし」を改める
  - [x] 8.3 `lapsed-suggestion-timing` の requirements 冒頭に、本 spec が置き換えた旨を追記
  - [x] 8.4 `docs/adr/0003` の Consequences（「`TimerFact` には出さない」）を改訂。`.kiro/steering/timer-model.md` の「駆動オーダーの保持」を「実装済み（`TimerFact.orderItem`・lift-group-display）」へ
  - 実測・2026-09-05: 文書のみ 7 ファイル（コードとテストは触っていない）。(8.1) `slot-suggested-start/requirements.md` の判断 5・Requirement 1.2・2.3〜2.5 に「本 spec が改めた」の追記——釜ごとの最早 1 件 → 表示できる群の品目を複数並べ押せるのは先頭だけ、時期の語は空か `now` で薄 / 濃の相が時期を伝える、壁時計不使用は時刻の語ごと不在へ強めた。tasks に無い判断 4（AC 1.2 の出所）にも同じ旨を足した——AC だけ改めて出所の判断が旧いままだと文書の中で矛盾するため。(8.2) `online-cook-scheduling/design.md` の 3 箇所——client 変更の表「`config` の受け」と「配信するが利用は強制しない」の段落を、`ClientView` の 3 項目に写し `pairSlots` の距離の計算に読む旨へ改訂（盤面のレイアウトには依然として用いない、を明記）。表の直下の「自動開始しない」の段落（レビュー追記で追加）は錨の繰り上げの撤去へ改訂。(8.3) `lapsed-suggestion-timing/requirements.md` 冒頭に置き換えの注記（撤去した `suggestionTiming` / `SuggestionTiming` / `planAnchor` とテスト 2 本の名を明記し、以下は撤去前の記録として残す）。(8.4) ADR-0003 は frontmatter の specs に `lift-group-display` を足し、表題直下に「表題の『client ワイヤには出さない』は改めた」の注記、Consequences の当該行を取り消し線で残して `TimerFact.orderItem` へ載せた事実（用途は Group_Started の判定だけ・走行中カードに卓は出さない・`Ordered` 撤去・不正は復号失敗 / 欠如は null）を記録した。`timer-model.md` の「駆動オーダーの保持」を「（実装済み）」にし、経過（`online-cook-scheduling` の engine 専用 `Ordered` → `lift-group-planning` が `tableId` を内側へ → `lift-group-display` が共有事実 `TimerFact.orderItem` へ昇格）を添えた。**計画からの追加 1 点**：本 spec の design が実装と食い違っていた箇所を改めた——Architecture 表の `queueDisplay.ts` 行と Component 3 の手順 1 は `boilSecondsOf` を export するとしていたが、実装は `suggestedItemOf` に一連を集め `boilSecondsOf` を非公開に戻した（task 0 の実測）ので実装注記へ。naming ゲートの表の下に、表に無く実装で増えた公開名 3 つ（`suggestedItemOf` / `compareArrival` / `displayName`・事後承認を要る）を表とは別に列挙した（表の承認済みの名と混ぜない）。`pnpm fmt` は差分なし。typecheck 0（`worker-configuration.d.ts` を除く）・lint エラー 0（警告は既存のみ）・fmt:check 通過・229 ファイル 1454 件通過（task 7 から変化なし）。**レビュー追記・2026-09-05**：指摘 3 件（実質 2 件——1 と 2 は同じ段落）をいずれも妥当と判断して直した。(a) `online-cook-scheduling/design.md` の「自動開始しない」の段落（8.2 で改めた表の直下）が「client は `startAt` を表示する」「錨を現在時刻へ繰り上げて表示する（`lapsed-suggestion-timing`）」のまま残っていた——f21bbab で撤去した経路を語る記述で、同じ節の 2 箇所を直して隣の段落を見落としていた。改訂注記を足し（可視の語は空か `now`・先頭だけ濃く・AC 8.2 の自動開始しないと AC 7.5 の `hasLapsedStart` は変わらない）、8.2 の箇所数を 3 に改めた。(b) 本 spec 自身の ADR-0005（2026-09-04）が翌日のレビューで改まった判断 16・17・19 を映していなかった——「最早の群だけ」（→ Visible_Groups）・「薄くても押せる」（→ Group_Head だけ）・「最初の 1 本が始まった」（→ Group_Started の定義）。ADR-0003 と同じ形式（表題直下の注記・取り消し線・「（2026-09-05 改訂・判断 n）」）で改訂した。「始まった」の文は嘘ではなく定義が精密化されただけなので取り消し線は引かず注記だけにした。(c) requirements 判断 1 の「他の群は出さない」にも判断 19 が広げた旨を添えた。文書のみ・コードとテストは触っていない。
  - _Requirements: 7.1〜7.4_

- [x] 9. チェックポイント — 全体の green と実機の見え方
  - レビュー追記・2026-09-05: 通しの確認（typecheck / fmt:check / lint 0 エラー / 229 ファイル 1454 件）はユーザーが実測。指摘 2 件を反映——(P1) `liftGroups.ts` の群の鍵に生の NUL が 4 つ入り git がバイナリ扱いにしていたのを `\u0000` のエスケープに直した（実行時の文字列は同一）。(P2) config の `unitOrigins` の数が `unitCount` と違うと `pairSlots` → `position` が描画中に落ちる境界の穴を、`toStoreConfig` が長さの一致を要る形で閉じた（`position` の doc に client 側の根拠を追記・wire.example に例）。軽微：AC 5.3 に planning 側の差し戻しが同じブランチに入る旨を注記。提案の `role="group"` とボタンの aria-label の重複は据え置き（判断はユーザーに委ねる）。
  - 実測・2026-09-05（自動検査のみ・実機は未）: `pnpm typecheck` 0 エラー、`pnpm lint` エラー 0、`pnpm fmt:check` 通過、`pnpm test` 229 ファイル 1454 件通過（workflow の申告を鵜呑みにせず本体セッションで再実行）。実機（`pnpm dev`・2 端末）の見え方の確認はユーザーに委ねる。
  - `pnpm typecheck` / `pnpm lint` / `pnpm test` / `pnpm fmt:check`。
  - 実機（`pnpm dev`・2 端末）で：同卓 3 品で先頭だけに丸ボタン、他は薄いラベル。先頭を押すと残りが錨に揃って次の先頭が濃くなる。別卓の群は先頭の群の 1 本目が始まるまで出ない。ラジアルに待ち行列の帯。degraded で提案も帯も消える。

- [x] 10. 申し送り（本 spec の範囲外・記録のみ）
  - 実測・2026-09-05: 下の 2 点に加え、実装で判明した事実を記す。(1) 容量分割 (i) で 3 本を同時に始めると Boil_Sync（arms 2）が 324 / 324 / 396 秒に散らすため、design の「360 秒に 3 本が boiled」は一度の発火では起きない（横断 Example は実際の発火の連鎖 324 → 360 に従う。client の結論は design どおり）。(2) 発火後の再計画は残りを startAt 同値で置くので、群の並びは到着順で決まる（AC 1.4 の同値規則）。(3) happy-dom は `getBoundingClientRect` が 0 を返すため、ラジアルの帯の左右配置は Example では通らず、`verticalColumn`（弧の上下の広い側）だけが描画される。左右配置は実機で確認する。
  - 麺種プリセットのアドホック開始（`connection.start`）は起点の釜の占有を検査しない既存経路のまま（design Component 3）。
  - 設定変更直後の hydration snapshot は旧 adjustment の錨で推奨を出す（`lift-group-planning` tasks の申し送り）。

- [x] 11. 実機の差し戻し（判断 20・`lift-group-planning` 判断 18・19・ADR-0008）——群の所属と開始の事実を engine から読む
  - 実測・2026-09-05: `liftGroups` は `group` で束ね `started = anchor > corrected`。`TimerFact.orderItem` の 2 コミットは衝突なく revert。生成器は batch ごとに `group` と `anchor`（同じ実効 endTime の走行中を併せて置く・skew 0 / −5 / +3 秒）を振り、`foreign` の仲間は意味を失ったので落とした。性質は「群は `group` 以外から導かない」「分割点は表示できる群の `anchor` だけ」「started は `anchor` と Corrected_Now だけの関数」へ。横断 Example の場面 3（keepsAnchor）は h_i（Short 300 秒 → 30 秒）の窓で意味が変わった——錨が 605 / 570 秒へ動いても一片は窓の内側で残り started のまま（client に `serveAt === anchor` が要らない実証）。届かなくなる場面は 547 秒（53 秒差）で別に置いた。ADR-0008 と `recommend` の doc の「A ≤ serveAt ≤ A + h_i」は実装（±h_i・判断 18「前後どちらでも」）に合わせて直した。230 ファイル 1467 件通過（本体で再実行）。
  - [x] 11.1 `liftGroups.ts`：群は `recommendation.group` で束ね、`started = anchor !== null && anchor > corrected`。`LiftGroup` は `{ group, anchor, items, started }`
  - [x] 11.2 `TimerFact.orderItem` の撤去（task 1 の 2 コミットを revert）。engine の `Ordered` は元のまま。ADR-0003・steering の記述を戻す
  - [x] 11.3 テストの追随：`generators.ts`（batch ごとの `group` / `anchor`）、`liftGroups.{property,example,crosslayer.example}`、`generators.smoke`、`slot-board-suggestions.example`、`radial-queue.example`
  - [x] 11.4 design の Component 1 / 2・判定表・性質表・naming 表を判断 20 へ
  - [x] 11.5 チェックポイント
  - _Requirements: 1.2, 1.7, 5.1〜5.3, 6.1, 6.3, 6.10, 6.12_

