# 次の作業（2026-09-15 朝）

2026-09-14 の作業の続き。**朝いちばんに確かめるべきことから並べる。**

## 0. まず 5 分で：対応表の投入が効いたか

`pos-menu` Policy を **2026-09-14T13:24:56Z** に投入した（中盛 1.5 玉：2 釜 → 1 釜・全 200 店）。
**効いたことをまだ確認できていない。**

`slotSpan` は**注文が届いた瞬間に `OrderItem` へ焼き付けられる**ので、設定を変えても既存の待ち行列は
2 釜のまま残る。`ORDER_LIFETIME_MS` が 2 時間なので、**15:24 UTC 以降の局面**で判定できる。

```
窓：直近 45 分。yude-men-cpsat-planner-dev の cpsat-plan-failed のうち scene を持つ行の scene.spans
```

| 2 釜の割合 | 結論 |
|---:|---|
| **1〜2 割** | 伝播も焼き付けも設計どおり。**中盛の修正は効いている** |
| **25% 以上** | **伝播を疑う番**（投入直後は 25〜39% だった） |

**失敗が 1 件も無ければ `spans` は取れない**（`scene` は失敗時にしか出ない）。その場合はそう言う。

## 1. `no-solution`（status=UNKNOWN）の原因——**未解明**

本番の求解失敗の本体。**走行中がある局面でだけ起き、走行中 1 本でも起きる**（1108 の実測で
走行中 1〜9 本のすべてに分布）。2026-09-14 に 5 つの仮説を立てて**全部外した**。

| 外した仮説 | 反証 |
|---|---|
| 走行中が増えると難しい | 走行中 0〜9 本の掃引で失敗 0。むしろ OPTIMAL が増える |
| 釜が埋まると難しい | 空き釜 12 → 3 本でも解ける |
| 上がりの時刻が近接すると難しい | 45 秒の窓に集中させても解ける |
| 釜の重複 | ローカルでは落ちるが、**本番は `INFEASIBLE` 0 件**・実測した局面も `slotCollisions` 0 |
| 満杯 | 釜 12 本に走行中 12 本でも解ける（超過すると落ちるが本番では起きない） |

**残る候補は hint の実行不能**（貪欲 hint は 18 局面中 1 つで実行不能だった実測がある）。
局面が取れるようになったので、**失敗した局面をローカルに組んで `--fix-hints` で解く**のが次の一手。

`cpsat-plan-failed.scene` に載っているもの：`slotCount` / `freeSlots` / `slotCollisions` / `arms` /
`liftWindowSeconds` / `running[{slots, endsInMs, boiled}]` / `spans`。

## 2. 12 日前の boiled タイマーが釜を塞いでいる

`yamaokaya-1125` の局面に、**-1,043,026 秒（≒ 12.07 日）前に茹で上がったまま Complete されていない**
タイマーが 2 本ある。釜 0 と釜 1 を永久に占有し、CP-SAT には `unavailable`（使えない釜）として渡る。

仕様どおりの状態（`boiled` は Complete 待ち）だが、**現場に効いている可能性が高い**。
放置されたタイマーをどう扱うかは決まっていない。

## 3. 未配備のもの

**ADR-0015（1 釜 1 Timer の不変条件）が未配備である。** コードも spec も試験も入っているが、
**アプリの配備を挟むと画面の WebSocket が切れる**ので見送った。

```
src/engine/decide.ts       遷移の結果に不変条件を当てる
src/engine/rejection.ts    SlotOccupied
docs/adr/0015-...          判断と却下 6 案
```

配備すると、使用中の釜への開始が `SlotOccupied` で拒否される。通常操作では client が idle の釜しか
押させないので見え方は変わらないはずで、弾かれた回数は `command-rejected` に残る。

## 4. 判断待ち

| # | 内容 |
|---|---|
| a | `tools/regenerate-menu-policy.mjs` ——`pos-menu-policy.json` を正本にしたので**生成は不要**。ただし「同じコードが複数のメニューで違う `slotSpan` を持つ」検査は今回の直し漏れ（19502）を実際に見つけた。**検査に書き換えるか、消すか** |
| b | `GET /admin/policies/{policyId}` が無い。**設定の正本が読めない**ので、今回は差分が分からないまま全置換した。次も同じ状況になる |
| c | 対応表に無いコード 10 種（正本 CSV にはある）。**麺量と認識されず茹で対象から外れる** |
| d | コード注釈の日付 115 箇所が `2026-09-13`（実際は 09-14）。昨日の正しい記述と混在するので一括置換できない |

## 5. 積み残した本線

**7-B（計画品質の比較）は全部やり直しである。** 卓の読み違い（`table_no = 1`）の上で測った値なので、
E1〜E9・順位相関・対象上限の掃引がすべて無効になった（`table-id-unknown-20260914.md` §6）。

**伝票ごとの待ちを目的にする案**（ユーザー提案）は手順 2 まで進んだ。実測では
「費用を足すだけでは効かない・予算も原因ではない・**出発点（hint）を変えると効く**」まで分かっている。
広がりは 5,042 → 1,650 秒（TS は 45）。待ちと逆転は既に TS を上回っている。

## 6. 今日の配備（すべて反映済み）

| Worker | 版 | 中身 |
|---|---|---|
| `yude-men-timer` | `67ad915b` | 保持の滑り・拒否のログ・伝票ごとの群 |
| `yude-men-cpsat-planner-dev` | `976fe2a1` | 失敗局面の記録・lead の窓の修正・hint の手伝い・釜の安定 |
| Policy `pos-menu` | 13:24:56Z 投入 | 中盛 2 釜 → 1 釜（**効果は確認待ち**） |
