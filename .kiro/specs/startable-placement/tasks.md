# Implementation Plan

前提：main（#34 まで）。永続の版は上げない（v12 のまま）。`pending-order-expiry` と同じ進め方（task ごとに `[-]` → `[x]` と実測・チェックポイント・コミット）。

- [x] 0. naming ゲート（design の表）をユーザーが承認する（2026-09-06 承認：`occupiedSlotsOf` / `pinNow` / `Pinned` / `cannotStart` / `operationScenes`）
  - `occupiedSlotsOf`（domain）、`pinNow` / `Pinned`・`cannotStart`（engine 内部）、`operationScenes`（tests）

- [x] 1. 事実の述語と harness
  - 実測・2026-09-06: `src/domain/store.ts` の `slotOf` の隣に `occupiedSlotsOf(timers: readonly { readonly slotIds: readonly string[] }[]): ReadonlySet<number>`（Timer の載る釜・補集合が Startable_Slot・engine の `Timer` も wire の `TimerFact` も満たす入力）を置き、client の `occupiedSlots(view)` は `occupiedSlotsOf(view.timers)` を返す薄い包みにした（`headsOf` / `displayableItemsOf` / `pairSlots` の呼び手は不変）。`tests/client/liftGroups.example`（+3：集合そのもの・6 品 6 釜の提案が落ちる釜 = `occupiedSlotsOf`・`pairSlots` が null の釜 = `occupiedSlotsOf` ちょうど）。`tests/core/operationScenes.ts`（新設・純粋）：`kitchenOf`（engine の params と client の config を同じ値で）・`step`（decide → Broadcast snapshot・拒否と no-op は throw・`alarmAt` を添える）・`viewOf` / `groupsOf` / `suggestionsOf` / `suggestionSummaryOf` / `displayedHeadsOf`（表示の順）・操作 `startHeads`（表示された先頭を提案の釜で）・`fire`（茹で上がり）・`completeOn(state, slot, now)`（釜 N の Timer を Complete）・`nextBoilEndOf`・駆動 `operate(kitchen, from, policy)`（刻みごとに「発火（茹で上がりの時刻で）→ 猶予を過ぎた boiled を昇順／降順に 1 釜 Complete → 表示された先頭を n 本開始」を踏み trace を返す）・性質 4.2 の述語 `startableGapOf(kitchen, step)`（表示の順で最初の `startAt ≤ now` の品目について、Timer が無く採用済み接頭辞の配置（推奨に一致する採用済み一片の配置で茹で時間帯が重なるもの）が予約していない釜が slotSpan に足り、上がりを含む窓の負荷が arms + HELPER_ARMS 以下なのに head の提案が一つも無ければ空白。「今」の品目が無ければ主張しない。合流は「計画が既に今に置いた事実」で足りるので別に検査しない。遷移の直後にだけ当てる）・`gapsOf(trace)`。`tests/core/startable-placement.example`（新設 5 件・4 通過 + 1 `it.fails`）：観測事実 8 を engine の実走で再現——開始 0 / 3 / 6 / 9 / 42 / 45 秒（Boil_Sync が 0・3 秒開始を 57 秒、6・9 秒開始を 67.5 秒へ揃え、上げ窓 [57, 102) が 4 本で埋まる）、発火 57 / 67.5 / 103.5 / 136.5 秒、72 秒（57 + 15）に釜 1 を Complete した直後は釜 0 が boiled（57 秒）・釜 1 が空き・釜 2・3 が boiled（67.5 秒）・4・5 が走行中で、残り A（o6#0）は釜 0 に「今」（72 秒・先頭群）、B（o7#0）は釜 1 に「今」（後続群）、提案は空、空白は `{at: 72, head: "o6#0", placedOn: [0], startable: [1], occupied: [0,2,3,4,5], groups: [["o6#0"],["o7#0"]]}` の 1 箇所だけ（45〜67.5 秒の全釜占有は空き釜不足の例外）。75 秒の釜 0 の Complete で再開（A が釜 0 に now → 75 秒に開始、B は 78 秒）し、8 品は最後まで処理され待ち行列も Timer も残らない。望む状態「72 秒の直後に A が釜 1 に「今」で先頭・空白なし」は `it.fails`（task 3 が `.fails` を外す）、修正前の観測は「【修正前の観測・task 3 が 0 箇所へ置き換える】」の 1 件に明示。typecheck 0（worker-configuration.d.ts を除く）・lint 0 errors（警告は既存）・fmt:check clean（428 files）・全数 245 ファイル 1680 テスト通過 + 1 expected fail。
  - [x] 1.1 `src/domain/store.ts` に `occupiedSlotsOf(timers)` を置き、client の `occupiedSlots(view)` をそれに置き換える（結果不変を `liftGroups.example` で見る）
  - [x] 1.2 `tests/core/operationScenes.ts`：decide → snapshot → decideView → liftGroups / slotSuggestions の連続処理 harness と、「今割当可能な Startable_Slot が先頭品目に足りるのに提案が空」の述語（例外：空き不足・予約の排他・上げ窓・合流）
  - [x] 1.3 観測事実 8 の 8 品の再現を `startable-placement.example` に赤で固定（72 秒の釜 1 の Complete の後に提案が出ない）
  - _Requirements: 1.1（事実の共有）, 4.3_

- [x] 2. 合成の失効
  - 実測・2026-09-06: `src/engine/commit.ts` の `committedSchedule` は `occupied = occupiedSlotsOf(running)`（domain の述語）を一度作り `livePrefix(accepted, targets, now, occupied, initial, lifts, members, presets, params)` へ通す（署名は不変・`baselineSchedule` へはまだ渡さない＝task 3）。`hasLapsedStart(slice, now)` は `cannotStart(slice, now, occupied)` ＝ 過去開始（`startAt < now`）∨ 押せない釜（`startAt ≤ now` かつ `slotIds` のどれかが `occupied` に在る）に置き換え、注記に「人が始めなかった事実」と「押せない釜に Timer が残っている事実」の両方と、開始時刻が先の配置は見ない（boiled は Complete される予測・判断 2）こと、ゲートは読まない（判断 5）ことを書いた。判定は従来どおり一片単位。`recommend.ts` / `cook-scheduling.integration` の注記の名を追随。`tests/core/commit.example`（+5・6 釜・釜 3 に置く一片で「維持なら釜 3・置き直しなら自前解の釜 0」が語る）：(a) boiled の釜 3 に `startAt === now` → 落ちて尾部が釜 0 に今、(b) Timer の無い釜 3 に `startAt === now` → 維持、(c) 30 秒後の将来配置は受領時点（boiled でも）維持され、30 秒後の遷移で釜 3 がまだ boiled なら落ちて釜 0 に置き直し・Complete 済みなら維持、(d) `startAt < now` は Timer が無くても落ちる、(e) 走行中の Timer でも `startAt === now` なら落ち、開始時刻が先なら見ない。`tests/core/commit.property` の期待側 `prefixLength`（実装の述語を呼ばない素朴な算術）に「`startAt ≤ now` かつ釜に走行中の Timer」を足した——boiled の釜は解放 `now` ゆえ生成器の採用済み計画が高い頻度で boiled の釜に「今」置くため、旧モデルは Property 6 で 181 場面目に反例（接頭辞の長さの食い違い）を出した。Property 20（feasible）は不変で通過。8 品の再現（採用済み一片なし）は task 2 では変わらず `it.fails` のまま。typecheck 0（worker-configuration.d.ts を除く）・lint 0 errors・fmt:check clean（428 files）・全数 245 ファイル 1685 テスト通過 + 1 expected fail。
  - [x] 2.1 `commit.ts`：`livePrefix` に `occupied` を通し、`hasLapsedStart` を `cannotStart`（過去開始 ∨ `startAt ≤ now` かつ釜に Timer）に広げる。`committedSchedule` が `occupiedSlotsOf(running)` を一度作る
  - [x] 2.2 `commit.example`：boiled の釜に `startAt === now` の採用済み一片は落ちる／Timer の無い釜なら残る／時刻の到来で「今」になった将来配置も次の遷移で落ちる
  - _Requirements: 3.4, 3.5_

- [x] 3. 2 段の計画
  - 実測・2026-09-07: `src/engine/schedule.ts` の `baselineSchedule(pending, release, members, lifts, presets, params, now, occupied, changeContext)`（`occupied` を `now` の隣に）は、1 段目（`seedOf` → `chooseSchedule`＝従来の忠実／候補比較を総費用で選ぶ）→ `pinNow` → 2 段目（同じ `chooseSchedule` に `Pinned` を通す）。「今」の品目が無いか配分が 1 段目と同じ釜なら 1 段目をそのまま返す。呼び手は `commit.ts`（`occupiedSlotsOf(running)` を接頭辞の失効と尾部の両方へ）・`src/solver`（`occupiedSlotsOf(request.running)`）・テスト 12 ファイル（`scheduleScenes` / `schedulingScenes` / `schedule.example` / `schedule.property` / `commit.property` / `admit.example` / `recommend.property` / `order-expiry-independence.property` / `lift-split.example`。場面の Timer から `occupiedSlotsOf(timers)`、Timer の無い場面は `occupiedSlotsOf([])`）。`pinNow` は design どおり——「今」（`startAt ≤ now`）の品目を表示順（`startAt` → `compareArrival`）に、`pool`（`release ≤ now`）の上で排他的に（`claimed`）配る：今割当可能な釜が足りれば空き釜だけから（前回の釜 → 1 段目の釜 → `chooseSlots`（空き釜だけを `now` にした表））、足りなければ待つ釜だけから（1 段目の釜 → 前回の釜 → index）、それも足りなければ `pool ∖ claimed` を index 順（退避先）。`Pinned` は `placements` / `notBefore` に加えて **`rank`（1 段目の配置の並び）**を持ち、2 段目の一片は 1 段目と同じ並びで返す（固定分を先頭に出すと同じ配置なのに並びだけ違う計画になり、Property 5.6 の `toEqual` が落ちた）。**design Component 3 からの変更点 4 つ（いずれも既存の性質の実測で判明）**：(1) 固定配置は上げ表には始めに全部載せる（(f) のため）が、**解放表には 1 段目で置いた位置と同じ位置で載せる**——後の群の固定配置の釜は手前の群に対して取り置き（`buildSchedule` の `reserve`）、群の内側でも合流した固定配置は合流の最初の判定の回の間、batch の固定配置は自分の batch の間は取り置いて（`placeGroup` の `reserve` → `openReserved`）、その後に `serveAt` で空ける。batch の切り方も固定配置を含めた正準順序の幅（1 段目と同じ）。始めに全部載せると、手前の卓の一片が後の単独品の固定した釜 0 を「上がった後」（65〜110 秒）に使い、また同じ回の合流分が固定した合流分の釜 3 を 52 秒から使い、一片の順に表を進めて組み直す次回の 1 段目（前回に忠実な候補）がそれを再現できず総費用が悪化する（120 秒／変更費用 5 秒・Property 5.6 の 2 つの反例）。取り置いても手前の群・同じ回・同じ batch の品目は 1 段目より釜を失わない（1 段目でそれらは固定配置の釜を使っていない——使えばその釜は「今」に空かない）。(2) 下限は **batch も合流分も置いた後の配置時刻に**当てる（`raiseToFloor`）——`assignSlots` の `earliest` に当てると batch の錨 max(earliest) が列全体を最遅の下限へ引きずり、1 段目が窓で 100 / 106 秒に分けた Thin 2 品が両方 106 秒になる（Property 5.7 の反例）。列の錨には下限の**最小**を足す（`placeBatch` の `bound`——固定した品目が抜けて max(earliest) が下がった列を 1 段目と同じ時刻から置く。どの品目も自分の下限より手前には来ないので何も失わない）。`raiseToFloor` は下限の昇順に、確定した配置だけを載せた表で `firstFit`（仮の時刻を載せると前回の有無で 90 / 93 秒に割れた・Property 5.7 の 2 つ目の反例）。下限より後ろの配置は品目ごとには戻さない（試したが、batch の成員は錨に揃うので 1 品だけ戻すと相方と時刻が割れ、1 段目の規則が作らない形になって次回が再現できず総費用が悪化した——Thin 2 品の卓で 1 品を 165 → 160 秒に戻し、次回は相方が 160 → 165 秒・Property 5.6）。代わりに **列の幅に、列から抜けた固定配置のうち錨の時刻に上がる分を数える**（`placeWithLifts` の `fixedSpan`・`placeBatch` の `fixedSpanAt`）——固定分が抜けて幅が上限に収まると、1 段目が「窓の残り」で切った先頭の塊（上限 4 で固定 1 本 ＋ 3 本）を arms 2 で 2・2 に切り直し、同じ窓に入っていた品目が次の窓へ遅れる。遅れは feasible だが、空いた窓へ次回の計画が別の品目を「今」に引き込み、その釜の配分が変更費用を生んで同じ入力の再計画が総費用で悪化した（Property 5.6 の反例：5 秒の業務改善に 12 秒の変更費用）。幅の勘定を 1 段目と同じにすれば切り方も同じになる。(3) `placeGroup` の batch の幅 `capacity` は解放が有限の釜だけを数える——取り置いた釜を数えると固定分が抜けて組み直した batch が置ける釜より広くなり、無限大の釜を取って `serveAt` が無限大になり `liftOverflow` の走査が止まらない（全数テストが停止しなくなった）。(4) `placeGroup` は `{ placements, placed }` を返し、呼び手は置いた分だけで表を進める（上げ表は本数を数えるので固定分を重ねない）。成員表（走行中の錨）には足さず局所費用の `members` にだけ足す点、`joinable` / `joinTarget` / `placeWithLifts` / `chooseSlots` の同点処理・`visibleGroupsOf` / `headsOf` / `initialRelease` / `admit` / 永続は不変。harness（`operationScenes`）に方針 `forgetShownPlan`（各遷移の前に Shown_Plan を忘れる）と `adoptCommitted`（各遷移の後に確定計画を採用済み一片に見立てる・settle と同じ導き方）を足した。`tests/core/startable-placement.example`（58 件）：8 品の再現が緑——72 秒の釜 1 の Complete の直後に A（o6）が釜 1 に「今」で先頭・B（o7）は待つ釜 0 へ退避・提案 `[[1, ["o6#0 now"]]]`・空白 0 箇所、A は 72 秒・B は 75 秒に始まり発火は 57 / 67.5 / 103.5 / **133.5** 秒（修正前は A 75・B 78 秒で 136.5 秒——期待値が動いた唯一の箇所で、今回の意図した効果そのもの）。レビュー反例 3 件：(i) 卓 X（−3 秒 Thin・−1 秒 Long）と卓 Y（−2 秒 Long）・空き釜 0 だけ → 空き釜は表示で先の Y の Long に渡り、X の Long は boiled の釜で今・Thin は 540 秒後、(ii) 採用済み B が Timer の無い釜 1 を 30〜90 秒に予約 → A は boiled の釜 0 で今のまま・harness の述語も空白と数えない、(iii) 釜 5 だけ空き・釜 0・1 が boiled・A/B（卓 X・Thin）が今・D（卓なし・Thin）が釜 5 に今・C（卓 Y・Long）が 60 秒後 → A が釜 5・B が釜 1・D が釜 0 へ退避し、C は 60 秒のまま（今にならない）。Component 3 の反例 2 件：固定した Long を成員に足さないので同卓の Thin（540 秒後）に錨は付かない／走行中 3 本が 60 秒に上がる窓で 105 秒へ延期された Thin の合流（開始 45 秒・錨 60）は、卓なし Long の再配分の後も残る。24 品の連続処理 48 通り（卓なし／同卓／4 品ごとの卓 × slotSpan 1／2 × Complete 昇順／降順 × Shown_Plan あり／なし × 採用済み接頭辞あり／なし・3 秒刻み・猶予 15 秒・二周目以降まで）で例外に当たらない空白 0 箇所・24 品は最後まで処理され待ち行列も Timer も残らない（全 48 通りで 1.3 秒）。`tests/core/startable-placement.property`（3 件・各 300 回・3 回再実行で全通過）：4.1（1 段目の「今」の表示先頭は、今割当可能な釜が足りれば Timer の無い釜に `startAt ≤ now` で置かれ、`liftGroupsOf` → `visibleGroupsOf` → `headsOf` の先頭に現れる）、4.6（1 段目に「今」が無ければ計画は `occupied`——場面の Timer でも任意の部分集合でも——に依らない）、4.7（「今」の集合は 1 段目と等しく・各 `startAt` は 1 段目以上・固定配置どうしの釜は相異なる・時刻は有限）。既存の性質は `schedule.property`（Property 1 / 2 / 15 / 1・16 / 7.8 / 17 / 3・14 / 5.7 / 5.4）・`commit.property`・`admit.property`・`recommend.property`・`lift-split`・`continuous-input`・`plan.example`・`settle-*`・`commit.example`・`admit.example`・`schedule.example` とも期待値を変えずに通過（上の変更点 (1)〜(3) は 5.6 / 5.7 / 全数の停止で検出し、性質を弱めずに実装側を直した）。**期待値の符号化を変えた性質は `schedule.property` の Property 5.6 と 5.7 の 2 つ（主張は変えない）。** 原因は同じ——2 段目の下限（AC 1.8・前倒ししない）は 1 段目の釜の組から出た時刻を残すので、釜の組が変わって解放が早まった品目は「自然な firstFit の時刻」より下限の分だけ後ろに置かれ、前回を手がかりに一片の順で組み直す計画（前回の配置を再現する分割は塊を前回の提供時刻で切るが、時刻は今の表の firstFit で決める）がその分だけ早い時刻を見つける。すなわち 2 段目の計画は「自分自身を前回として組み直す」ことの不動点ではない（次回が同じ入力で真に良い計画へ 1 歩進み、そこで落ち着く）。5.6（同じ入力で続けて計画すると同じ計画か総費用が真に下がる計画）：旧符号化「Change_Cost 0 ⇒ `toEqual`」を「2 回目は配置が一致すれば同じ計画、一致しなければ総費用と業務費用が悪化しない（秒の整数の同点を許す——1 ミリ秒の改善や、5 秒の業務改善に 5 秒の変更費用が同点になる）。3 回目は 2 回目と同じか真に下がる（落ち着く）。Change_Cost 0 なら総費用は前回以下」に置き換えた。Change_Cost は窓の内側の移動と遠い将来の移動（遠さで割って 0）を数えない（plan-stability 判断 2 (c)(d)）ので、変更費用 0 と配置の一致は元々同値ではなく、旧符号化は主張より強い条件だった（実測：Thin の釜が 53.001 秒に空く釜 3 から 53 秒に空く釜 2 へ動き次回は 105.001 → 105 秒・変更費用 0・総費用 2 ミリ秒改善／卓なし Thin が 60 秒空きの釜 4 から 52 秒空きの釜 5 へ動き次回は 60 → 52 秒・変更費用 0（遠さの割引）・業務費用 8 秒改善。8〜15 回に 1〜2 回の頻度）。5.7 (i)（前回の釜が表の外なら時刻は前回の無い計画に一致）：**占有なし（1 段目）で見る**——前回の釜の第一候補は 1 段目の `assignSlots` の規則で、(i) はその規則が効かないときの一致を言う。占有なしなら配分は 1 段目の釜をそのまま採り 2 段目は組まれないので主張は従来どおり厳密（実測：占有ありでは卓 t-1 の 2 釜の Thin が固定配置の釜を避けて 75 秒から 80 秒の窓へ動き、卓 t-2 の 2 品は下限で 80 秒に残るが、前回を持つ計画は空いた 75 秒の窓に置く）。(ii)（塞げばハード制約を守る）は占有ありのまま。なお 5.7 (i) には本 task と無関係の稀な反例が元々在る（15 回に 1 回程度・main の `schedule.ts` を当てても同じ場面で同じ結果：12 釜・卓 t-1 の 2 釜 Thin が前回の釜を表の外へ写した文脈で 30 秒から 76 秒へ動く）——ここでは触らず報告に留める。2 段目で下限を自然な時刻まで緩めることは AC 1.8（各 startAt は 1 段目以上）に反するので実装側では直さない——user 判断が要れば「下限は今にならない範囲に留める」案が在るが、それでも前回の時刻を手がかりにする組み直しは下限で残した時刻を再現しないので、不動点でないこと自体は残る。typecheck 0（worker-configuration.d.ts を除く）・lint 0 errors（警告は既存）・fmt:check clean（429 files）・全数 246 ファイル 1742 テスト通過（expected fail 0）。
  - [x] 3.1 `baselineSchedule(…, now, occupied, changeContext)`：1 段目 → `pinNow` → 2 段目（配分が同じなら 1 段目を返す）。呼び手（`commit.ts` / `src/solver` / テスト・`scheduleScenes` / `schedulingScenes`）に `occupied` を通す
  - [x] 3.2 `pinNow`：表示順（`startAt` → `compareArrival`）、`pool`（`release ≤ now`）の上の**排他的な割当**（`claimed`）。今割当可能な釜が足りる品目は空き釜だけから（前回の釜 → 1 段目の釜 → `chooseSlots`）、足りない品目は待つ釜（boiled）だけから（1 段目の釜 → 前回の釜 → index）、それも足りなければ混ぜて index 順（退避先）。固定配置どうしの釜の重複が無いことを性質に
  - [x] 3.3 `buildSchedule(…, pinned)`：固定した配置を先に解放表・上げ表へ載せる（**卓の成員表＝走行中の錨には足さない**。局所費用の `members` にだけ足す）。`placeGroup` は固定した品目をそのまま出力し、残りに下限を当てる——batch は `earliest`、合流は置いた後の配置時刻（`joinable` / `joinTarget` は変えない）。忠実／候補比較の両方に通す。レビュー反例（固定配置を成員に足すと 60 秒麺に `anchor: 600` が付く／窓延期の合流 45 → 105 秒が残る）を例示に
  - [x] 3.4 テスト：`startable-placement.example`（8 品の再現が緑・レビュー反例 3 件：卓 X/Y の表示順配分・予約の排他で A は待つ・C は「今」にならない）、`startable-placement.property`（4.1・4.6・4.7）、24 品の連続処理（昇順／降順・Shown_Plan 有無・採用済み接頭辞有無・同卓／別卓／卓なし・slotSpan 1／2）で例外に当たらない空白 0
  - [x] 3.5 既存の性質（`schedule.property` / `commit.property` / `admit.property` / `lift-split` / `continuous-input` / `plan.example` / `settle-*`）がそのまま通る。期待値が動く場合は理由を実測に書く
  - [x] 3.6 チェックポイント（typecheck / lint 0 errors / test / fmt:check）とコミット
  - _Requirements: 1.1〜1.8, 2.1〜2.3, 3.1〜3.3, 4.1, 4.2, 4.4〜4.7_

- [ ] 3′. 2 段目の改訂と保持候補 R（2026-09-07・レビュー判断。task 3 は「実装済み・受入未了」）
  - [x] 3′.1 `isStale` の対象集合を「置ける品目」に（`plan-stability` Requirement 7）——定義を一箇所に置き、自前解・合成・ゲート・復元が同じ集合を使う。回帰：未知麺種を含む卓の外部計画が採用できる／設定変更で再び置けるようになると以前の一片が欠落で落ちる
    - 実測・2026-09-07: `src/engine/schedule.ts` に `placeableTargets(pending, now, presets, params)`（＝ `planTargets` を `isPlaceable`（茹で時間が引ける ∧ `slotSpan ≤ liftCap`）で絞る。切ってから絞るので 65 件目は繰り上がらない・空き不足と上げ窓の混雑は除外理由にしない）を置き、`isStale` の呼び手すべてがそれを渡す——合成 `committedSchedule`（`livePrefix` の接頭辞判定と尾部の `remaining`）・ゲート `prune`・復元の試作 `restoreScenes.ts`（`targetsOf(scene)`）・`self-solution-gate.probe`。自前解は `placeGroup` が群の中で同じ `isPlaceable` で絞る。**`buildSchedule` の群は正本の `planTargets` から組んだまま**——群の順（卓の最早到着）は置けない品目も数える従来どおり。置ける品目から群を組むと `schedule.example` の性質 5.6 の回帰（Ghost を先着に持つ卓 t-2 が先頭に並ぶ反例）の並びが変わって期待値が動くので、集合の定義（Requirement 7）と並びの規則を分け、後者は変えなかった。`digestInput` と `RequestPlan.pending`（`settle.ts`）は `planTargets` のまま——「何が計画対象か」と「そのうち何が置けるか」は別の問いで、spec（online-cook-scheduling 11.2 の改訂・pending-order-expiry 判断 5）は要求と指紋の範囲を正本の計画対象と定めており Requirement 7 はそれを変えていない。指紋は計画対象の麺種のプリセットを畳むので、麺種が再び置けるようになれば要求は出る。回帰：`schedule.example`（+4：65 件で 5 件目が未知麺種・10 件目が span 5 → 置ける品目は 62 件で 65 件目は入らない／自前解の品目集合＝置ける品目・各一片は置ける品目に対して `isStale` 偽／プリセット追加・arms 3 で集合が広がる／全釜を 10000 秒塞いでも集合は同じで自前解は待たせて置く）、`admit.example`（+4：(a) 未知麺種 G を含む卓 t-m の外部計画（M を今）が採用される、(b) G をプリセットに足すと同じ一片は欠落で落ち・G も置く一片は通る、(c) span 5 は arms 2 で集合に無く採用・arms 3 で欠落、(d) 置けない G を置いた一片は混入で落ちる）、`commit.example`（+3：採用済み一片は G が在っても維持／プリセット追加で落ちて自前解が M・G を置き直す／span 5 も同じ）、`plan.example`（+2：受領で採用され Persist・プリセット追加で状態も Effect も動かない）。typecheck 0（worker-configuration.d.ts を除く）・lint 0 errors（警告は既存）・fmt:check clean（433 files）・全数 249 ファイル 1766 テスト通過 + 5 expected fail（既存の `plan-stability-occupancy.example` 4 件・`self-solution-gate.probe` 1 件）。`schedule.property` / `admit.property` / `commit.property` は 3 回とも通過（`schedule.property` 単体でさらに 6 回通過。作業中に Property 17（keepsAnchor）が 1 回落ちたが、自前解の経路は述語の写し替えだけで配置は不変ゆえ、`self-solution-gate.probe` の場面 E と同じ既存の稀な反例（`raiseToFloor` の下限・3′.4 で撤去）と見る）。
  - [x] 3′.2 共有の述語：`cannotStart`（保持の条件）と解放の feasibility を合成・ゲート・復元で一つに。`tableKeyOf` の公開（または `restoreSchedule`）
    - 実測・2026-09-07: `tableKeyOf` は `src/engine/project.ts`（成員表 `tableMembers` の隣・同じ NUL 始まりの単独キー）に置き、`schedule.ts` が再公開する（`boilMillisOf` と同じ形——`schedule.ts` は `stability.ts` を値で読むので、`stability.ts` から `schedule.ts` を値で読めば循環になる）。`stability.ts` の `completeWithShown` は局所の `order.tableId ?? key` を `tableKeyOf(order)` に置き換えた（同値：卓ありは tableId、卓なしは単独キーで、鍵は一片を束ねる Map と「いま置いている一片」との照合にしか使わず、単独キーがいま置いている一片の鍵に一致する品目は既に置かれているので来ない。`stability.*`・`schedule.example` の 5.x は変わらず通る）。`cannotStart(slice, now, occupied)` は `schedule.ts`（`isStale` の隣）へ移して公開し、`livePrefix` と `restoreScenes.ts` が同じ関数を呼ぶ。注記に「採用済み・復元した一片を**保持する**条件であって、生成した計画全体の成立の条件ではない（判断 13・空き釜不足で boiled の Complete を待つ配置は合法・AC 1.4）」。解放の feasibility は `feasibleRelease(placements, release, targets, presets): SlotRelease | null`（(a)(b)(c)(d) ＋ 茹で時間の一致・`targets` に無い品目を指す配置も落とす）を `schedule.ts` に公開し、ゲートの `prune` は (e) `keepsAnchor` → (f) `withinLiftCap` → `feasibleRelease` の順に同じ位置の表で呼ぶ（`admit.ts` の局所関数は撤去）。復元の試作 `reasonOf` も同じ関数を呼ぶ（`feasibleOnRelease` の写しを撤去）。**`livePrefix` は解放表の feasibility を当てていない**（`isStale` / `cannotStart` / `keepsAnchor` / `withinLiftCap` の 4 つだけ。採用済み一片は採用時にゲートで通っており、その後の変化はこの 4 つが見る）——Requirement 6 / 7 は採用済み接頭辞にそれを求めないので足さなかった（`commit.property` の期待側 `prefixLength` もその契約で書かれている）。復元（3′.3 の `retain`）は判断 10 のとおり `feasibleRelease` を当てる。`restoreScenes.ts` の写し 3 つ（`tableKeyOf` / `cannotStart` / `feasibleOnRelease`）と局所の置ける品目の絞りを撤去し、`zz-restore.probe`・`self-solution-gate.probe` は変更なしに通る。チェックポイントは 3′.1 と同じ数値（3′.1 と同じファイル群を跨ぐため 1 コミットに束ねた）。
  - [ ] 3′.3 `retain`（復元 → retime → 一片ごとに検証 → 不正はその位置で再生成）と `baselineSchedule` / `committedSchedule` の R 経路。R と F を同じ旧 Shown_Plan の総費用で比べ R ≤ F なら R。`Continuity.faithful` の撤去
  - [ ] 3′.4 2 段目を「復元 → 検証 → 不正だけ再生成 → 新たな『今』を配分して固定」の loop に改め、`reserve`（後群の取り置き）・`fixedSpan`・`raiseToFloor` を撤去
  - [ ] 3′.5 テスト：`schedule.property` の 5.6 / 5.7 (i) を実占有（`occupiedSlotsOf(running)`）に戻し、配置の一致と Change_Cost = 0 を別々に検査（3 回目まで）。`plan-stability-occupancy.example` の 4 場面と `self-solution-gate.probe` の場面 E から `it.fails` を外す。`self-solution-gate` を性質に（完成した R・F が (c)(e)(f) と `isStale` を満たす）。**レビュー反例（同卓の先着 Short 60 秒・後着 Long 600 秒・釜 0 boiled・釜 1 だけ空き・他 4 釜は 1000 秒後：loop の再配分後に Short が釜 1「今」で先頭に出る。loop 発見順の固定では Short が釜 0 に落ちて提案が空）を固定 Example に**。`zz-restore.probe` は `retain` の実装に置き換える。摂動あり（2 秒経過・先頭開始・新着・Complete）で選ばれた計画の総費用が F 単独より高くならない・提案変更量の集計。task 3 の例（8 品・24 品・反例 3 件）と 4.7′ / 4.8
  - [ ] 3′.6 チェックポイントとコミット
  - _Requirements: plan-stability 6.1〜6.5, 7.1〜7.5, 5.6, 5.10, 5.11; startable-placement 1.8（改訂）, 4.7〜4.8_

- [ ] 4. 文書と最終ゲート
  - [ ] 4.1 `lift-group-planning`（`baselineSchedule` の署名・2 段・`initialRelease` の注記「Complete は釜の占有ではない」に「開始の可否は別」を添える）、`lift-group-display`（`occupiedSlots` は domain の共有述語）、`plan-stability`（前回の釜の第一候補は 2 段目の配分に吸収）、`online-cook-scheduling`（合成の失効に開始を妨げる配置）に日付付きの注記
  - [ ] 4.2 ADR-0012：予測（解放表）と事実（Timer の無い釜）を分け、「今」置く配置の釜の選択にだけ事実を読ませる。Considered Options：boiled を占有として解放表に載せる／連鎖を緩めて後続群を出す／同点処理だけ／client だけで直す
  - [ ] 4.3 全数チェックポイント（typecheck / lint 0 errors / test / fmt:check）
