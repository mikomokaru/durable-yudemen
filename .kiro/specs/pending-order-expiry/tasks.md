# Implementation Plan

前提：main（#33 まで）。永続の版は上げない（v12 のまま）。`plan-stability` と同じ進め方（task ごとに `[-]` → `[x]` と実測・チェックポイント・コミット）。

- [x] 0. naming ゲート（design の表）をユーザーが承認する（2026-09-06 承認：`liveOrders` / `ORDER_LIFETIME_MS` / `livePending`）
  - `liveOrders` / `ORDER_LIFETIME_MS`（`src/domain/order.ts`）、`livePending`（client 局所）

- [x] 1. 述語と定数（domain）
  - 実測・2026-09-06: `src/domain/order.ts` に `ORDER_LIFETIME_MS = 2 * 60 * 60 * 1000` と `liveOrders(pending, now)`（`arrivalTime + ORDER_LIFETIME_MS > now` の filter・並びを保つ・全件期限内なら入力と同じ配列を返す）を `compareArrival` の隣に置いた。他の src は触っていない。`tests/domain/order.example.test.ts`（13 件：定数 2 時間・ちょうど寿命は含まない・1 ms 手前は含む・now が 1 ms 進めば切れる・未来の到着は期限内・並びを保つ・入力を変えない・全件期限内は同じ参照・空は同じ参照・全件期限切れは空・1 件切れれば新しい配列・決定的・arrivalTime だけで判定）と `tests/domain/order.property.test.ts`（5 件・各 300 runs：性質 5.1 冪等（二度目は同じ参照）・5.2 単調・5.3 並び（入力の部分列・重複なし）・述語の半開区間と入力の不変・全件期限内なら同じ参照）。typecheck 0（worker-configuration.d.ts を除く）・lint 0 errors・fmt 済・`domain-imports` / `no-wake` / `no-backfill` の静的検査 31 件通過・domain の 2 ファイル 18 テスト通過。
  - [x] 1.1 `src/domain/order.ts` に `ORDER_LIFETIME_MS`（2 時間）と `liveOrders(pending, now)` を置く（並びを保つ・半開区間・全件期限内なら同じ参照）
  - [x] 1.2 `tests/domain/order.example` / `order.property`：冪等・単調・並び保持・境界・同じ参照（性質 5.1〜5.3）
  - _Requirements: 1.1〜1.5_

- [x] 2. engine の入口
  - 実測・2026-09-06: `planTargets(pending, now)` は `liveOrders` → 正準順序 → 先頭 64 件（絞ってから切る）。`baselineSchedule(pending, release, members, lifts, presets, params, now, changeContext)` と内部の `buildSchedule` に `now: EpochMillis` を足し（`changeContext` の直前。null の経路でも要るため引数）、`committedSchedule` / `admit` / `digestInput(pending, running, params, now)`（`now` は絞るためだけ・畳まない）/ `settle` の要求抑制（`planTargets(confirmed.pendingOrders, now)`）へ通した。`isStale` / `livePrefix` は不変。`snapshotMessage` は `pendingOrders: liveOrders(state.pendingOrders, now)`（Broadcast と hydration は同じ関数）。`ChangeContext.pending` は 4 入口で各自の `now` の Live_Orders——`settle.deriveRecommendations`（`live` を一度導き `committedSchedule` の pending と文脈に）・`plan.receivePlan`（`live` を合成・admit・文脈に）・`admit`（冒頭で `liveOrders(pending, now)`・文脈・採点 3 回・段 1 の `planTargets`・段 2 の合成の全部に）・`src/solver`（`liveOrders(request.pending, now)` を自分の時計で、計画と文脈の両方に）。`start.ts` の照合は `liveOrders(state.pendingOrders, args.now).find(…)`（消費 `consumeOrder` は正本に対して）。受理・`isSamePending` / `isSameConfirmedResult`・永続・client・Alarm は触っていない。既存の呼び手（src 2 箇所・テスト 11 ファイル 96 箇所）に `now` を通した（場面の時刻 `NOW` / `now` / `scene.now`）。**混在の例示の形（design からの追記）**：期限切れの旧先頭 A・生きている次品目 B（大盛 2 釜）と同卓の C・走行中の仲間 M が 600 秒に上がる表（arms 1・L 45）。前回の提案は A を 1 秒前（釜 5）・B を今（釜 2・3）・C を 45 秒後（釜 4）。正しい文脈（A を除いた pending）では B が旧 Head で、B・C を両方 45 秒後へ pack する計画は業務費用 45 秒の改善に先頭の変更 2L = 90 秒が乗って総費用で劣り（分割 1634 対 pack 1589 + 90 = 1679）、B は今に残る。A を文脈に残せば旧 Head は A になり pack の変更費用は 0 に消え、pack が採られる（自前解の再現・`baselineSchedule` に両方の文脈を渡して確認）。design の「B を 1 秒遅らせる計画」は純粋な `changeCost` の例（`stability.example`・90 対 0）として置き、4 入口は自前解と Acceptance_Gate を実際に通す形にしたため、遅れは 1 秒ではなく上げ窓 1 つ分（45 秒・h_i 60 秒の内側ゆえ (d) は 0）である。場面は `tests/core/expiryScenes.ts`（`mixedScene(now)` / `changeCostOf` / `startOffsets`）に一つ置き、solver は `Date.now()` で組む。テスト：`tests/domain/order.{example,property}`（task 1）、`schedule.example`（+7：64 件の期限切れが先頭に在っても生きている先頭 64 件・並びに依らない・1 ms 手前は期限切れ側で埋まる・自前解も同じ 64 件・混在 3 件）、`schedule.property`（Property 5.4・200 runs）、`settle-order-expiry.example`（新設 13 件：Broadcast と hydration の `pendingOrders` = `liveOrders`（性質 5.5）・全件期限内は参照同値・`RequestPlan.pending` に期限切れ無し・指紋不変・性質 5.6 無害（Broadcast が空の待ち行列と等しく要求しない・hydration も等しい）・性質 5.7 不変（返る状態は同じ配列・Persist は全件）・no-op は正本の比較・時間経過は遷移にならない・混在 3 件（hydration と settle で B が今・Persist の Shown_Plan・要求は B と C・pack の費用 90 対 0））、`plan.example`（+6：期限切れを指す一片は `isStale` で落ち道連れ・生きている一片は採用され snapshot は生きている分・採用済みの期限切れ一片は合成が捨て自前解が埋める・混在 3 件（pack 棄却・Shown_Plan 空なら採用・費用 90 対 0））、`admit.example`（+5：`isStale`・正本を渡しても冒頭で絞る・混在 3 件（棄却・空なら採用・総費用 −45 + 90））、`tests/solver/index.example`（新設 2 件：DO binding と waitUntil を差し替えて fetch → 202 → deliverPlan、B は solver の今・C はちょうど 45 秒後・A は無い／全件期限切れは空の計画）、`digest.example`（+4：期限切れは指紋に現れない・64 件でも枠を食わない・`now` は畳まない・期限を跨げば変わる）、`stability.example`（+3：B を 1 秒遅らせる計画 90 対 0・期限切れ自身の変更は 0）、`start-order-item.example`（+3：ちょうど寿命は `OrderItemNotFound`・1 ms 手前は開始・消費は正本）。snapshot / inline snapshot の期待値は変えていない（`timer-model.static` の `pendingOrders: []`・統合テストの到着は `Date.now()` 相対で期限内）。typecheck 0（worker-configuration.d.ts を除く）・lint 0 errors（警告 63 は既存）・fmt:check clean（423 files）・全数 241 ファイル 1657 テスト通過。
  - [x] 2.1 `planTargets(pending, now)`（絞ってから切る）。`baselineSchedule` / `buildSchedule` に `now` を足し、`committedSchedule` / `admit` / `digestInput(pending, running, params, now)` / `settle` の要求抑制へ通す
  - [x] 2.2 `snapshotMessage`：`pendingOrders` を `liveOrders(state.pendingOrders, now)` にする（Broadcast と hydration の両方）
  - [x] 2.3 `ChangeContext.pending` を 4 入口で絞る：`settle.deriveRecommendations` / `plan.receivePlan` / `admit`（冒頭で `liveOrders(pending, now)`・文脈と採点と `planTargets` の全部に使う）/ `src/solver`（`request.pending` を自分の `now` で）。混在の例示（期限切れの旧先頭 A・生きている B・B を 1 秒遅らせる計画の変更費用が 2L = 90 秒、A を残すと 0）を 4 入口で固定
  - [x] 2.4 `start.ts` の照合を `liveOrders(state.pendingOrders, args.now)` に替える（消費は正本に対して）
  - [x] 2.5 テスト：`schedule.example` / `schedule.property`（性質 5.4 枠）、`settle-*.example`（性質 5.5 一致・hydration）、`plan.example` / `admit.example`（期限切れを指す一片は `isStale`・要求に乗らない）、`start-order-item.example`（`OrderItemNotFound`・`now` だけ違う 2 本）、`stability.example`（対応から外れて費用 0）、性質 5.6（無害）・5.7（不変）
  - [x] 2.6 チェックポイント（typecheck / lint 0 errors / test / fmt:check）とコミット
  - _Requirements: 2.1〜2.8_

- [x] 3. client の入口
  - 実測・2026-09-06: `queueDisplay.ts` に局所 `livePending(view, corrected)`（`liveOrders(view.pendingOrders, corrected)` を返すだけ・内部で補正しない）を置き、`orderQueueEntries(view, units, now)` は境界として `correctedNow(view.offset, now)` を 1 回計算し、`livePending(view, corrected)` を到着順に並べ、`suggestedItemOf(view, recommendation, corrected)` を呼ぶ。`suggestedItemOf` は第 3 引数 `corrected` を受けて `pendingItemOf(livePending(view, corrected), …)` で引く（寿命を過ぎた品目を指す推奨は「待ち行列に無い推奨」と同じ経路で null）。`liftGroups(view, corrected)` は持っている `corrected` をそのまま渡す。引数名は `now`＝ローカル・`corrected`＝補正済みで固定し、`ClientView.pendingOrders` は wire のまま（絞った値を状態にしない）。`SlotBoard` / `OrderRail` / `RadialMenu` は触っていない（`orderQueueEntries` / `liftGroups` の結果を読むだけなので入口は増えない）。新しいタイマーは足さず既存の秒 tick に乗る。テスト：`order-queue.example`（+6：snapshot 直後は残り `correctedNow` が寿命を跨ぐと消える・境界は半開区間（ちょうど寿命は含まず 1 ms 前は含む）・判定は補正後現在時刻（offset +30 秒でローカル時計より先に切れる）・期限切れを指す推奨は提案にならず `suggestedItemOf` も境界で null・**非ゼロ offset（−120 秒）の一致テスト 2 件**：左レールと `slotSuggestions(visibleGroups(liftGroups(view, corrected)), view, corrected)` が 1 ms 前は同じ 2 品目・ちょうど寿命は同じ 1 品目を見て、−2/−1/0/+1 ms の 4 点で集合が一致）、`liftGroups.example`（+3：1 ms 前は 3 品の群・ちょうど寿命で long が群から抜ける・釜 0 の head が消え mid/short が先頭 arms 2 本へ繰り上がる・境界は domain の述語そのもの）、`radial-queue.example`（+1：SlotBoard 実描画・offset +1 秒・Date.now が 1 ms 進んで補正後現在時刻が寿命に達すると開いたままの帯とレールから A が同時に消え、花びらは残る）、`order-expiry-agreement.property`（新設 2 件・各 200 runs・性質 5.8：`genLiftScene` に offset（±600 秒）と品目ごとの寿命の位置（ちょうど／1 ms 前／1 ms 過ぎ／元のまま）を混ぜ、左レールの集合 = `liveOrders(view.pendingOrders, corrected)`、群 ⊆ レール、レールに在りプリセット既知の推奨は群に在り `suggestedItemOf` も非 null、釜の提案 ⊆ レール）。`liftGroups.property` の `suggestedItemOf` 呼び出し 2 箇所に `corrected` を通した。静的検査（`pending-order-list-left-rail.static` の `orderQueueEntries` 3 引数・`lift-group-display.static`・`domain-imports`）は不変で通過。typecheck 0（worker-configuration.d.ts を除く）・lint 0 errors（警告は既存）・fmt:check clean（424 files）・全数 242 ファイル 1669 テスト通過・新設 property は 3 回連続通過。
  - [x] 3.1 `queueDisplay.ts` に `livePending(view, corrected)`（補正済みを受ける・内部で補正しない）を置き、`orderQueueEntries(view, units, now)` は境界として `corrected` を 1 回計算して `livePending` と `suggestedItemOf(view, recommendation, corrected)` を呼ぶ。`liftGroups(view, corrected)` は持っている `corrected` をそのまま渡す（時刻の引数名は `now`＝ローカル・`corrected`＝補正済みで固定）
  - [x] 3.2 テスト：`order-queue.*` / `liftGroups.*` / `radial-queue.*`——snapshot 直後は残り、`correctedNow` が寿命を跨ぐと左レールとラジアルから消える（性質 5.8）。**非ゼロの `offset`** で左レールと釜の提案が同じ品目集合を生きているとみなす一致テスト（境界の 1 ms 前後で同時に切り替わる）
  - [x] 3.3 チェックポイントとコミット
  - _Requirements: 3.1〜3.3_

- [x] 4. 走行中の独立（テストだけ・構造は変えない）
  - 実測・2026-09-06: src は触っていない。`tests/core/order-expiry-independence.property.test.ts`（新設 2 件・各 250 runs・3 回連続通過）。場面は `schedulingScenes` と同じ素材（`genRunning`：卓あり／卓なし・走行中／茹で上がり済み・0〜5 本、`genOrderSpec`：0〜4 注文、`genParams`・arms 1〜4・toleranceRatio 1〜30、`now` = NOW − 5 秒〜+60 秒）で、生きている状態 `alive`（自前解を採用済み一片と Shown_Plan に載せる）と、待ち行列の `arrivalTime` だけを `ORDER_LIFETIME_MS + 700 秒〜4 時間` 過去へ動かした `expired`（Timer・設定・採用済み一片・Shown_Plan は同じ参照）の対。前提として `liveOrders(alive.pendingOrders, now)` が入力そのもの・`liveOrders(expired.pendingOrders, now)` が空であることを毎回確かめる。操作は `decide` を入口に Event の全種から `StartOrderItem` だけを除く 10 種（アドホック `Start`・`Cancel`／`Complete`／`Adjust`（既存 id と不在 id）・`AlarmFired`・`Reconcile`・`OrderArrived`・`OrderCancelled`・`RecordsReceived`（既存注文への後着・新規・0 件の除去・端末 2 つの順序の印）・`PlanArrived`（生きている待ち行列の自前解））。主張：可否と拒否事由が等しい・`state.timers` が等しい・実効 endTime（`adjustedEndTime`）が等しい・`tableMembers` が等しい・Alarm 効果（SetAlarm／ClearAlarm）は両方が列を出せば等しく、出た Alarm は相手の Timer から導いた `nextAlarmEffect` に等しい（no-op の有無は確定結果の比較と採用・要求の経路で片方だけ変わりうるため、Alarm が Timer だけの関数であることをこの形で固定）・Persist の snapshot の Timer が等しい。hydration は `toWireSnapshot` の `timers` と `serverTime` が等しく `pendingOrders` は片方だけ空（AC 4.4）、確定の `settle` でも Timer・実効 endTime・`tableMembers`・Alarm が等しい（Boil_Sync の結果は待ち行列に依らない）。**design からの追記**：Alarm の等しさは「両方出れば等しい」に加えて `nextAlarmEffect` との照合で固定した（`PlanArrived` は生きている側だけが採用して列を出し、期限切れ側は全一片が `isStale` で無変化になる場面が実在するため、Effect 列の Alarm を無条件に等しいとは言えない）。typecheck 0（worker-configuration.d.ts を除く）・lint 0 errors（警告は既存）・fmt:check clean（425 files）・全数 243 ファイル 1671 テスト通過。
  - [x] 4.1 `tests/core` に性質 5.9：Timer・設定・`now`・操作を固定し、待ち行列の `arrivalTime` だけを寿命以上過去へ動かした二状態に、両状態で同じに成立する操作（発火・完了・調整・キャンセル・Boil_Sync・アドホック開始・Record 受理・外部計画の受領・hydration）を与え、`timers`・実効 endTime・Alarm 効果・`tableMembers` が等しい。`StartOrderItem` は含めない（期限切れ品目の開始拒否は 2.5 の例示で別に見る）
  - _Requirements: 4.1〜4.4, 5.9_

- [x] 5. 文書と最終ゲート
  - 実測・2026-09-06: src・tests は触っていない（文書のみ）。5.1：`online-cook-scheduling` requirements（Requirement 2 の snapshot は Live_Orders・「全量」は生きている品目の全量／Requirement 5 の `digestInput(…, now)` は絞るためだけで畳まず抑制条件は不変／Requirement 11.2 の計画対象は Live_Orders → 正準順序 → 先頭 64 件・絞ってから切る）と design（`digestInput` の署名・アルゴリズム手順 1・ワイヤ snapshot の「超過分も含む全量」の読み替え・Property 15）、`pos-order-ingress` requirements（Requirement 8 の末尾に `arrivalTime` が寿命の起点になった追記・受理は不変・最早の `arrivalTime` を引き継ぐ規則ゆえ後着で生き返らない・`removeOrder` 後の非空の後着は新しい起点で入り本 spec は保証しない）、`lift-group-display` design（描画ごとのデータフローと Component 4 に client の入口・時刻の契約 `now`＝ローカル／`corrected`＝補正済み・`suggestedItemOf(view, recommendation, corrected)`・ラジアルは同じ導出）、`plan-stability` requirements（Requirement 2 の末尾に「消えた品目」に期限切れを含める改訂・混在の 90 対 0）と design（Component 2 の `context.pending` は 4 入口の Live_Orders）に、既存の `> **改訂（…・2026-09-06）:**` の形で日付付きの注記を置いた。5.2：`docs/adr/0011-expired-orders-are-filtered-not-removed.md`（status accepted・specs pending-order-expiry・0009 / 0010 と同じ長さと調子。Considered Options は Alarm で状態から除去・受理時に弾く・費用にする／薄く見せる・設定化・client だけで絞る。Consequences は正本の伸び（未決 1 (a)）・観測値なし・時計のずれの前提・後着の帰結・署名に時計が加わる・絞った値は状態に持たない）。`no-backfill.static` の語彙は ADR にも現れない。5.3：typecheck 0（worker-configuration.d.ts を除く）・lint 0 errors（警告は既存）・fmt:check clean（425 files）・全数 243 ファイル 1671 テスト通過（task 4 と同数・文書のみの変更）。
  - [x] 5.1 `online-cook-scheduling`（計画対象の定義に `now`・snapshot の待ち行列は Live_Orders）と `pos-order-ingress`（`arrivalTime` が期限の起点になった）に日付付きの注記。`lift-group-display` design に client の入口の注記
  - [x] 5.2 ADR-0011：正本を変えず純粋関数で絞った値を正とする（Alarm を張らない・永続を触らない・走行中は独立）。Considered Options：状態からの除去（Alarm）・受理時に弾く・設定化
  - [x] 5.3 全数チェックポイント（typecheck / lint 0 errors / test / fmt:check）
