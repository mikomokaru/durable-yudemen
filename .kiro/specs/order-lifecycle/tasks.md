# Implementation Plan

前提：main（#37 まで）。永続は v12 → v13。`startable-placement` と同じ進め方（task ごとに `[-]` → `[x]` と実測・チェックポイント・コミット）。`lift-order-numbering`（番号・卓・品名・中断の色分け）は本 spec の後に載せる。

- [x] 0. naming ゲート（requirements の表 12 件 ＋ design の内部名）をユーザーが承認する（2026-09-07 承認）

- [x] 1. domain：`OrderItem` と導出
  - [x] 1.1 `PendingOrder` → `OrderItem`（`completedAt` / `interruptedAt` を足す）、`refersTo` / `isLive`、`itemStatusOf` / `pendingOrders` / `orderItemsToBroadcast` / `orderItemOf`（`liveOrders` は `pendingOrders` の内側へ）。型の改名は src / tests 全体（機械的）
    - 実測（2026-09-07）：`src/domain/order.ts` に `ItemStatus` / `isLive` / `refersTo` / `itemStatusOf` / `pendingOrders` / `orderItemsToBroadcast` / `orderItemOf` を足し、`toPendingOrders` → `toOrderItems`（内部の 1 件は `toArrivedItem`・厨房の事実は null で生まれる）。型名の改名は src / tests の 76 ファイル（機械的・`PendingOrder` の残りは doc の「旧 PendingOrder」1 箇所）。wire の `toOrderItemFromWire` は `completedAt` / `interruptedAt` を null か非負整数で受け、欠如は落とす（design Component 3 の decode をここで先に満たした。snapshot のフィールド名の改名と `TimerFact.orderItem` は task 2 / 3）。移行（`migrate.ts`）は v12 以前の品目を null で埋める形に留め、v13 の検証は task 2。`pnpm typecheck` 0 error（tests 含む）・`pnpm lint` 0 error・`pnpm test` 250 files / 1795 tests 全通過・`pnpm fmt:check` 通過
  - [x] 1.2 `tests/domain/order.example` / `order.property`：性質 7.1（排他・boiled は cooking）・7.3′（中断は状態に効かない）・`pendingOrders` と `orderItemsToBroadcast` の境界（1 時間 59 分開始・2 時間 1 分）・`orderItemOf` の null
    - 実測（2026-09-07）：example 14 件（排他・boiled → cooking・Timer が completedAt に優先・片方一致とアドホックは効かない・7.3′・`pendingOrders` の並びと同じ参照と冪等・1 時間 59 分開始 / 2 時間 1 分 snapshot で配信に載り計画に載らない・Complete 後と未調理期限切れは消える・`orderItemOf` の null 3 経路と engine Timer からの解決）、property 4 件（7.1 の排他・7.3′・`pendingOrders` = live ∩ unstarted ⊆ broadcast ∧ 参照先は必ず配信・同じ参照と冪等・各 300 runs）。`tests/domain` 8 files / 100 tests 通過、`order.example` + `order.property` の 3 回再実行はいずれも 37 / 37
  - _Requirements: 1.2, 1.7, 3.1, 3.2, 7.1, 7.3′_

- [x] 2. engine：状態・遷移・後着・永続
  - [x] 2.1 `TimerState.orderItems`（旧 `pendingOrders`）、`snapshot.ts`、`migrate.ts` v13（`pendingOrders` → `orderItems`・`completedAt` / `interruptedAt` の欠如は null・**注文品目が不正なら既存どおり `MigrationFailed`**）、`types.ts` 版、`docs/persisted-schema-rollback.md` v13 行（切戻しの注意：v13 で残した cooking / done が v12 では未着手として現れる）
    - 実測（2026-09-07）：`TimerState.orderItems` / `StoreSnapshot.orderItems`・`CURRENT_SCHEMA_VERSION = 13`。`migrate` は `record.orderItems ?? record.pendingOrders` を `reviveOrderItems` → `toOrderItem`（`completedAt` / `interruptedAt` は欠如 / null → null・有限数値はその値・他は番兵で全体 `MigrationFailed`）で読む。個別破棄はしない。`docs/persisted-schema-rollback.md` に v13 行（下り移行は `orderItems` を `pendingOrders` に戻す・cooking / done が v12 で未着手として現れる注意）と §2.3 の鍵名を追記。`timer-model.static` の鍵集合と inline snapshot（`orderItems: []`・`version: 13`・`TimerFact` に `orderItem`）を更新
  - [x] 2.2 `start.ts`：`consumeOrder` 撤去・照合は `pendingOrders`・`OrderItemCooking`。`complete.ts`：参照先に `completedAt`。`cancel.ts`：参照先に `interruptedAt`（上書き）。参照先なしは書かない
    - 実測（2026-09-07）：`startOrderItemTimer` は `pendingOrders(state.orderItems, state.timers, now)` で照合し、無ければ正本の同じ鍵が `cooking` なら `OrderItemCooking`（`rejection.ts` に追加）、他は `OrderItemNotFound`。品目は消費しない（`moved` は `orderItems` に触れない）。`completeTimer` / `cancelTimer` は対象 Timer を `find` し、`orderItemOf(timer, state.orderItems)` が在れば `completedAt` / `interruptedAt` を `now` で書き（同じ参照の品目だけ差し替え）、無ければ `orderItems` は同じ参照のまま
  - [x] 2.3 `pending.ts`：`upsertOrder(items, timers, arrival)` / `removeOrder(items, timers, externalOrderId)` に **Timer 集合を渡し**（欠落品目の unstarted / cooking は `itemStatusOf` で判定）、「注文属性だけ更新・厨房の事実と生きた Timer を保持・arrivalTime 引継ぎ・後着に無い品目は unstarted だけ除く」に。`isSamePending` → `isSameOrderItems`
    - 実測（2026-09-07）：`upsertOrder` は既存の同じ鍵に `withOrderAttributes`（noodleType / firmness / tableId / slotSpan / itemName / sizeName の 6 つだけ）を当て、到着に無い品目は `itemStatusOf !== "unstarted"` なら残し、新しい品目は当該注文の末尾（無ければ集合の末尾）に最早の `arrivalTime` で足す。`removeOrder` は unstarted だけ除く。`consumeOrder` / `startedItems` / 生きた Timer の除外を撤去。`isSameOrderItems` は 11 フィールド全部を比べる（旧 `isSameOrder` は slotSpan / itemName / sizeName を比べていなかった）
  - [x] 2.4 `settle.ts` / `plan.ts` / `admit.ts` / `digest.ts` / `receive.ts` / `order.ts`：`pendingOrders(...)` を読む。snapshot は `orderItemsToBroadcast`。`RequestPlan.pending` は `pendingOrders` の結果。`project.ts` `toWireTimer` に `orderItem`
    - 実測（2026-09-07）：`settle` は `deriveRecommendations` / 計画対象 / `ChangeContext.pending` に `pendingOrders(state.orderItems, state.timers, now)` を、`digestInput(orderItems, timers, params, now)` は内側で同じ絞りを、snapshot は `orderItemsToBroadcast(...)` を載せる。**wire のフィールド名は `pendingOrders` のまま**（改名は task 3。engine 側は `snapshotMessage` の 1 箇所に注記）。`plan.ts` / `admit.ts` は `pendingOrders`（`admit` は呼び手が絞った集合でも冪等）。`receive.ts` / `order.ts` は `state.timers` を `upsertOrder` / `removeOrder` に渡す。`toWireTimer` は `orderItem: { externalOrderId, itemIndex } | null`（tableId は写さない）。**`TimerFact.orderItem` を今足した**（`toWireTimer` の返り値が `TimerFact` ゆえ型が要る）。`Timer` は `Omit<TimerFact, "orderItem"> & Ordered`（engine の参照は tableId を持つ）。`wire.ts` の `toTimerFact` は `null` か `{ 非空 string, 非負整数 }` を検証し他は snapshot ごと落とす（design Component 3 の decode を先に満たした・tests/wire は task 3）。client は compile のため `persistence.ts` `toClientTimer` と `connection.ts` の暫定 Timer に `orderItem: null` を置いた（検証つきの復元は task 4）
  - [x] 2.5 テスト：`start-order-item.*`（消費しない・`OrderItemCooking`・done / 期限切れ / 不在は `OrderItemNotFound`）、`complete` / `cancel` の記録（参照先なし・`arrivalTime` 不変）、一括完了の各品目、`pending.example` / `received-order.example`（Requirement 2 の回帰 6 件：A 調理中に {A, B} 再送で A が残る・done は戻らない・Cancel → 再送で `interruptedAt` 保持・再開始→完了で done・次の Cancel でだけ上書き・後着に無い品目は unstarted だけ除く）、`settle-*` / `digest` / `plan` / `admit` の読む集合、`migrate.*`（v12 → v13・往復・**注文品目が不正なら `MigrationFailed`**（個別破棄しない）・参照先なし Timer が動き完了しても書かない・再送で補われた後は書く）
    - 実測（2026-09-07）：`start-order-item.example` に `OrderItemCooking` / done → `OrderItemNotFound` / 中断品目の再開始 / 後着で卓が移っても Timer は旧卓（品目は新卓・cooking）/ 開始は正本に触れず snapshot は cooking を載せる、`start-order-item.property` は「消費せず同じ参照・指した品目だけ cooking」に改めた。新規 `tests/core/order-lifecycle.example.test.ts`（13 件：complete / cancel の記録と参照先なし・期限は戻さない・系列 cooking → unstarted → cooking → done と `interruptedAt` の上書き・一括完了の各品目・1 時間 59 分開始 / 2 時間 1 分 snapshot・要求と推奨と指紋は未調理・v12 由来 Timer の発火 / 完了 / Cancel で書かない・再送で補われた後は書く）。`pending.example` を新規則で書き直し（回帰 1・2・6 と Cancel 保持・v12 参照先の補完・並び・`isSameOrderItems`）、`pending.property` の Property 21 は「生きた Timer の品目は正本に残り cooking・参照の整合」に改め、生成器の不変条件（started は集合と Timer の両方）を改めた。`received-order.example` に取り込み経路（RecordsReceived）での回帰 6 件。`migrate.example` に v12 → v13（読み替え・参照先なし Timer 保持・v13 往復・不正な `completedAt` / `interruptedAt` / 要素は `MigrationFailed`）、`migrate.property` に v12 → v13 の件数と並び・現行 snapshot 往復（各 200 runs）。`digest.example`（cooking / done は指紋に現れず、開始そのものは指紋を変える）・`plan.example`（調理中の品目を指す一片は全棄却）・`admit.example`（段 1 で落ちる対照）・`settle-order-expiry.example`（期限を超えた調理中は snapshot に載り要求には無い・アドホックは足さない）を追加。既存の場面の適応：`operationScenes` の終了条件を `pendingOrders(...).length === 0 ∧ timers 空` に、`adoptCommitted` の文脈を `pendingOrders` に改め（品目は消費されないので `orderItems.length === 0` は成り立たない）、`startable-placement.example` の「最後まで処理される」は `orderItems.every(completedAt !== null)` で読む。`expiryScenes` / `schedulingScenes` / `scheduleScenes` は鍵名の付け替えのみ。`continuous-input.example` は変更不要（`state.orderItems` の残りを見る主張は消費の有無に依らない）。client の TimerFact リテラル（14 ファイル）と生成器（`wireGenerators` は参照の有無を往復・`client/generators` は null）に `orderItem` を足し、raw snapshot の frame にも `orderItem: null` を足した（decode が欠如を落とすため）。`sync-set-batch-complete.static` は TimerFact 7 フィールド（行頭の `readonly` だけ数える）、`to-wire-timer-adjustment.example` は参照が鍵だけで tableId を出さないことを固定。
    - 実測（チェックポイント）：`pnpm typecheck` 0 error（tests 含む）・`pnpm lint` 0 error（警告は既存の no-shadow 系のみ）・`pnpm test` 251 files / 1834 tests 全通過・`pnpm fmt:check` 通過。property の 3 回再実行（schedule 5.6 実占有・startable-placement・order-expiry-independence・domain order・commit・admit・pending・migrate・stability・plan-retention・start-order-item・settle・receive の 13 files）はいずれも 67 / 67
  - [x] 2.6 チェックポイントとコミット
  - _Requirements: 1.1, 1.3〜1.6, 2.1〜2.6, 3.3〜3.4, 4.1〜4.3, 6.1〜6.3, 7.2〜7.6, 7.8〜7.9′_

- [ ] 3. wire と shell
  - [ ] 3.1 `messages.ts` / `wire.ts` / `timer.ts`：snapshot の `orderItems`・`TimerFact.orderItem` の decode（形の検証・不正は snapshot ごと落とす）。`store-timer-do.ts` / `solver/request.ts` の名前の付け替え。Operation History の `TimerFact` 読み手が追加フィールドを無視できることを確認
  - [ ] 3.2 `tests/wire/*`：往復と関門。`timer-model.static`（鍵集合・inline snapshot v13）・`offline-degradation.static`
  - [ ] 3.3 チェックポイントとコミット
  - _Requirements: 4.2, 4.4, 7.7_

- [ ] 4. client
  - [ ] 4.1 `connection.ts`：`ClientView.orderItems`。`queueDisplay.ts`：`livePending` → `pendingOrders(view.orderItems, view.timers, corrected)`。`slotDisplay.ts` / `SlotCard`：`orderItemOf` で品目を引ける口（表示は `lift-order-numbering`）。`persistence.ts`：`toClientTimer` が `orderItem` を検証して復元（不正・欠如は null に畳み Timer を失わない）
  - [ ] 4.2 `cancelGuard.ts` / `SlotCard`：残り < 60 秒の 1 タップは `complete`、それ以外は従来の `arm` → `cancel`
  - [ ] 4.3 テスト：`order-queue.*` / `liftGroups.*` / `radial-queue.*`（左レールとラジアルは `pendingOrders`）、`cancelGuard.*` / `slot-card.*`（`complete` / `cancel` の分岐・性質 7.4）、`persistence.*`（参照付き Timer の保存往復・旧 localStorage の欠如は null・不正は null で Timer を失わない）、`operationScenes` の系列（開始 → Cancel → 再開始 → 完了で `cooking → unstarted → cooking → done`・7.6）
  - [ ] 4.4 チェックポイントとコミット
  - _Requirements: 4.5〜4.6, 5.1〜5.2, 7.4, 7.6_

- [ ] 5. 文書と最終ゲート
  - [ ] 5.1 `pos-order-ingress`（後着は注文属性の更新・調理中の除外規則の撤去）、`online-cook-scheduling`（計画対象は `pendingOrders`）、`pending-order-expiry`（`liveOrders` は内側・二つの読む集合）、`plan-stability`（対応は未調理の間）、`lift-group-display`（`TimerFact.orderItem`）、`sync-set-batch-complete`（一括完了は各品目に `completedAt`）、`synchronized-boil-adjustment`（`Timer.orderItem.tableId` は開始時点の卓）に日付付きの注記。ADR-0003 の Consequences を改訂（wire に参照を出す）
  - [ ] 5.2 ADR-0013：品目は生涯を通じて残り状態は導出する・参照は Timer 側だけ・厨房の事実は `completedAt` / `interruptedAt`・後着は注文属性だけ・二つの読む集合・v12 の移行例外
  - [ ] 5.3 全数チェックポイント（typecheck / lint 0 errors / test / fmt:check）と既存 property の再実行（5.6 実占有・startable-placement・expiry・independence）
